# DiveEnd 长期自治开发前置清单

更新时间：2026-07-31

## 目的

这份文档定义 DiveEnd 长期开发中的责任边界，使 LLM agent 能够持续完成“理解现状、选择任务、实现、测试、桌面验收、更新文档、提交代码、报告结果”的闭环，同时把必须由项目所有者完成的身份认证、付费授权、法律确认和高风险操作保留为人工闸门。

本文按工作类型分类，不按开发阶段分类。项目现状和代码映射仍以 `README.md`、`AGENTS.md` 和 `docs/PROJECT_MAP.md` 为准。

## 已确认的运行决策

项目所有者已于 2026-07-30 确认：

- 第一目标用户是项目所有者本人，产品按个人研究者的本地优先桌面工作台设计。
- 功能设计 `F1-F8` 全部接受。
- 界面设计 `U1-U8` 全部接受。
- 允许 agent 自动创建分支、commit、push、创建 PR、启动本地服务并使用本地凭证执行受控真实 E2E。
- API 授权上限为每天 1 亿 token。强弱模型及 Python Screening 抽取共享持久化的每日计数器，请求前预留额度、请求后按 provider usage 结算；provider 不返回 usage 或请求失败时采用保守估算。达到上限后后端会在发送请求前停止。
- 不考虑向其他 Mac 用户分发，暂不申请 Apple Developer Program，也不开展签名和 notarization。
- 合并正式分支、公开发布、Git 历史重写、超预算付费和真实云数据破坏性操作仍保留人工闸门。

## 外部配置验证

2026-07-30 的脱敏真实请求结果：

| 服务 | 状态 | 证据与限制 |
| --- | --- | --- |
| 强 LLM | 可用 | `config/strong_llm.json` 的模型完成真实 chat completion；配置文件权限已收紧为 `0600`。 |
| 弱 LLM | 可用 | `config/weak_llm.json` 的模型完成真实 chat completion；配置文件权限已收紧为 `0600`。 |
| 百度网盘 | 可用 | 原 access token 已过期，正式客户端成功使用 refresh token 更新并以 `0600` 落盘；真实 E2E 完成数据库、PDF、manifest 上传、列举、下载和隔离测试目录清理。 |
| GitHub Git 远端 | 可用 | SSH 可读取 `qiaoyo/DiveEnd`，push dry-run 验证可创建远端分支。 |
| GitHub CLI | 可用 | `gh` 已通过 keyring 登录 `qiaoyo`；对 `qiaoyo/DiveEnd` 具有 `ADMIN` 权限和 `repo` scope，可用于创建 PR。 |
| Semantic Scholar | Key 不可用 | 2026-07-31 对同一论文详情接口复测：无 key 返回 `200`，当前 key 返回 `403 Forbidden`。配置为 `0600`、JSON 字段、40 字符 ASCII token 和 `x-api-key` 请求头均正确。`~/setup_proxy.sh` 已执行，但代理端点连接超时。需要 Semantic Scholar 支持或重新签发 key；代码继续保留 arXiv、缓存和明确失败提示。 |

2026-07-30 后续实现验证：

- F5 已在本地开发交付形态落地：`scripts/setup_pdf_service.sh` 创建忽略的项目 `.venv`，打包桌面应用自动启动服务、等待 readiness，并在退出时停止进程。
- F6 已落地为默认约 20 篇候选；只对排名最前的 4 篇执行 PDF 预取、解析和弱模型结构抽取，其余候选在选择、导入或阅读时按需处理。
- 重写后的最多 3 个检索 query 会全部执行，再按原始问题和扩展词的标题/摘要匹配度统一排序，不再由第一个填满上限的 query 独占结果。
- 强弱模型共享每日 1 亿 token 硬预算；当日使用量保存到数据目录 `.diveend/llm_usage.json`，设置页可查看使用量并调整上限。
- Python PDF 服务会汇总两次结构抽取的 provider usage 并返回 Go 后端，Screening 不再绕过共享预算。
- Semantic Scholar 的 `403` 被视为永久性来源错误，首轮即停止该来源；arXiv 部分结果继续完成研究地图，不再等待几十次无效重试。
- 桌面启动不再同步等待 PDF service 冷启动；服务在后台预热，DeepStart、DeepRead 和 Screening 首次需要 PDF 时统一等待 readiness。
- DeepRead 问答优先使用弱模型，真实验证约 4.33 秒返回有依据回答；全文总结保留强模型质量。长论文上下文按问题相关性和核心章节分配，模型返回的 section ID 和摘录必须能在输入上下文中验证。
- DeepRead 长请求支持前端主动取消和应用退出取消；已发送但失败或超时的模型请求保留保守 token 预留，避免潜在计费绕过每日上限。
- DeepStart 排序结果持久化标题/摘要命中解释；真实三-query 检索验证前五名均返回可核查的匹配原因。会话页统一到现有研究工作台 token，去模板化扫描命中由 78 降至 14，保留的命中是关键词高亮、真实层级标签和告警状态。
- Screening 的 PDF 抽取、首次 AI 筛选和后续决策生成都支持会话级非破坏性停止；停止会保留已完成论文和最后一次已提交选择。初始节点及后续选择使用 SQLite 事务同步提交节点、路径和论文状态，模型失败或取消不会留下半完成决策。
- Screening 页面已统一到研究工作台 token，去模板化针对性扫描命中为 0；浏览器演示流验证了导入、选项选择和固定操作栏，桌面宽度无横向溢出。
- Screening 页面会读取最近的持久化会话并按提取、筛选、已入库状态恢复；恢复已有节点不触发新模型请求，已入库会话不会重复显示导入动作。
- Sync 页面已统一中文操作语义和研究工作台 token，移除卡片嵌套、玻璃模糊和默认多色状态块；页面级去模板扫描由 87 个命中降为 0，11 条交互测试及浏览器无横向溢出检查通过。
- Sync 的六类状态探测允许部分成功，但不再把失败静默显示成全零；页面会列出连接、进度、冲突、记录、设置或恢复状态中读取失败的模块，并保留其余可用数据。
- DeepRead 阅读器外围已移除旧式多色状态块和层层圆角卡片，去模板扫描由 35 个命中降为 2；保留项是紧凑工具栏标题和真实 PDF 缩略图网格。章节依据可切换到对应解析章节，但 PDF 抽取数据尚无可靠页码锚点，不能承诺自动跳转到原始 PDF 页。
- Settings 控制面已统一到共享字段、面板和状态 token，去模板扫描由 62 个命中降为 8；保留项全部是 Radix 开关的圆形轨道和滑块。浏览器验证强弱模型、每日 1 亿 token 预算和固定保存栏可见且无横向溢出。
- Library 面板已将文件夹操作、批量选择、论文列表、移动和删除统一为紧凑资料管理样式，去模板扫描由 65 个命中降为 0；7 条文件夹与论文交互测试及生产构建通过。
- Go 最低补丁版本已提升到 1.25.12；旧的本机 1.25.5 被 `govulncheck` 识别出 9 条可达标准库漏洞，随后将 Windows-only 不可达告警对应的 `x/sys` 升级到 v0.44.0，完整扫描结果为 `No vulnerabilities found`，Go test/vet/race 和 Wails build 均通过。
- 2026-07-30 完整本地基线通过 Go test/vet/race、全部 70 条前端测试、18 条 PDF 服务测试、前端构建、Wails build 和秘密扫描；独立浏览器烟测在 1440、900、720 px 三档宽度覆盖发现、阅读、筛选、同步的 24 个可见状态且无控制台错误，窄窗口每张截图前均验证无页面级横向溢出。
- 2026-07-31 补完打包窗口验收：Computer Use 发起真实 DeepStart，Wails 事件从 3% 持续推进并完成 20 篇研究地图；DeepRead 加载 10 页 PDF，弱模型问答返回三条可核查原文证据；Sync 读取百度连接、200 条成功记录、0 冲突和 0 失败；Settings 读取强弱模型与 1 亿 token 日预算。

## 一、LLM Agent 可以自动化完成

### 项目治理与任务选择

- 阅读当前权威文档和实际代码，校正文档与实现之间的偏差。
- 维护 `README.md`、`AGENTS.md`、`docs/PROJECT_MAP.md`、测试说明和变更记录。
- 从已确认的产品目标、缺陷、测试结果和技术债中选择下一项高价值工作。
- 将历史规格视为背景资料，不让过期的 Marker、GORM 等描述重新进入当前实现。
- 检查工作区、分支、远端和忽略规则，避免把本地数据、构建产物或密钥提交到 Git。

### 产品与代码开发

- 在现有 React/TypeScript、Go/Wails、SQLite、Python/FastAPI 架构内实现前后端功能。
- 重构检索、AI 辅助阅读、总结分析、文库、筛选、同步和设置功能。
- 补齐加载、空状态、失败、重试、取消、恢复、离线和窄窗口等交互状态。
- 对第三方服务增加超时、限流、重试、缓存、降级、错误脱敏和可诊断信息。
- 维护数据库迁移、前后端类型、Wails bindings、配置模板和兼容逻辑。
- 在设计已对齐的范围内持续改善界面，不需要为每个像素调整重新等待确认。

### 自动测试与真实交互验证

- 运行 Go、前端和 PDF service 的单元测试、集成测试、构建、静态检查和密钥扫描。
- 为修复的缺陷和新增行为补回归测试。
- 启动 `wails dev` 或打包后的桌面应用，通过 computer-use 点击真实界面。
- 观察按钮、路由、事件、数据库、文件系统、网络请求和后端日志之间的数据流。
- 使用可重复的本地 fixture 验证检索、PDF 解析、AI 依据、笔记、导入和同步预览。
- 在本地凭证可用时执行受控的 Semantic Scholar、LLM 和百度网盘真实请求。
- 对外部网络失败与代码缺陷分别归因，不把供应商故障误判为产品成功。

日常 CI 默认不依赖付费模型或个人云账号。真实服务 E2E 应是显式触发、有限预算、避免破坏数据的验收任务。

### 版本控制与交付

- 以 `main` 作为唯一主开发基线；`archive/legacy-master-2026-07-31` 仅保留治理前无共同祖先的旧 `master` 历史，不从该归档分支继续开发。
- 在允许的分支范围内创建 `codex/*` 分支、提交、推送并维护 Pull Request。
- 提交前运行 `bash scripts/secret_scan.sh` 和与改动相称的测试。
- 生成变更说明、测试证据、已知限制和下一步建议。
- 建立 GitHub Actions，自动执行测试、构建、依赖审计和 secret scan。
- 生成内部测试包、校验文件、版本说明和 ZIP 归档。
- 发现失败后继续定位、修复、复测，而不是停在“命令失败”的状态。

### 持续自治工作循环

```text
读取权威文档和 git 状态
  -> 核对实现与实际风险
  -> 选择已授权范围内最高价值任务
  -> 创建或使用工作分支
  -> 实现前端、后端和必要迁移
  -> 自动测试与桌面交互验收
  -> 更新项目状态和代码映射
  -> secret scan
  -> commit / push / PR
  -> 报告结果、证据、风险和下一任务
```

### 默认需要保留的人工闸门

以下操作即使技术上可以自动执行，也不得默认自动进行：

- 合并到受保护分支或发布正式版本。
- 产生超出约定额度的付费 API 消耗。
- 删除、覆盖或大批量修改真实云端数据。
- 对外公开仓库、安装包、网站或用户数据。
- 接受平台协议、完成实名验证、付款或法律声明。
- 轮换、吊销或删除仍在使用的凭证。
- 重写 Git 历史、强制推送或更改协作者可见的提交历史。
- 开启遥测、上传论文内容或收集任何用户数据。

## 二、短期需要与你对齐的设计

下面记录会显著改变开发方向的设计决定。项目所有者已于 2026-07-30 全部接受，后续 agent 应直接按这些约束实施，除非项目所有者明确修改。

### 功能设计

| 编号 | 需要确认的问题 | 建议默认值 |
| --- | --- | --- |
| F1 | 第一目标用户是谁 | 先做个人研究者的本地优先桌面工具，不先承担团队协作和公共 SaaS 的复杂度。 |
| F2 | 核心任务的优先顺序 | 固定为“检索发现 -> 阅读理解 -> 有依据的总结分析”；筛选、文库和同步服务于这条主线。 |
| F3 | AI 输出的可信度契约 | 回答和总结必须给出论文依据、可跳转位置与局限；没有证据时明确说明，不生成伪引用。 |
| F4 | 模型与成本策略 | 保留一个强模型和一个低成本模型，供应商可配置；自动测试不用付费 API，真实调用有预算上限。 |
| F5 | PDF service 的交付方式 | 最终由桌面应用托管和自动启停，用户不手动运行 Python；启动失败时提供诊断和重试。 |
| F6 | 检索结果的产品取向 | 首屏优先高相关性的约 20 篇结果，展示来源和匹配理由，再允许扩大召回范围。 |
| F7 | 百度同步的定位 | 保留为用户主动开启的备份能力，不阻塞检索和阅读主流程，不默认上传论文内容。 |
| F8 | 隐私与遥测 | 默认无内容遥测；只有在单独对齐隐私说明后，才考虑用户可关闭的匿名崩溃信息。 |

### 界面设计

| 编号 | 需要确认的问题 | 建议默认值 |
| --- | --- | --- |
| U1 | 一级信息架构 | 保持“发现 / 阅读 / 分析”，历史、同步和设置作为工具入口，不增加营销首页。 |
| U2 | 阅读工作区 | 桌面端采用目录、PDF、AI/笔记三栏；左右栏可收起，正文始终是视觉中心。 |
| U3 | 窄窗口交互 | 小于约 1100px 时折叠目录与文库，PDF 和 AI 使用分段切换，避免横向挤压。 |
| U4 | AI 结果呈现 | 先给结论和依据，再展开推理细节、局限和相关章节；证据点击后定位到原文。 |
| U5 | 视觉风格与密度 | 使用克制、紧凑、适合长时间工作的研究工具界面；弱化装饰卡片和大标题。 |
| U6 | 文库入口 | 保留阅读页中的轻量论文抽屉，同时提供完整文库管理视图处理批量操作。 |
| U7 | 语言策略 | 界面以中文为主，论文标题、术语和原文保留原语言，翻译是可切换的辅助层。 |
| U8 | 操作反馈 | 长任务持续显示阶段、进度、取消和可恢复状态；成功反馈安静，失败反馈可行动。 |

### 自治权限

建议授予：

- 允许自动创建和切换 `codex/*` 分支。
- 允许在通过对应测试和 secret scan 后自动 commit。
- 允许自动 push `codex/*` 分支并创建 PR。
- 允许在本机启动服务、桌面应用和测试进程。
- 允许使用本地测试凭证执行有限次数的真实 E2E。

建议继续人工批准：

- 合并 PR。
- 正式发布。
- Git 历史重写与强制推送。
- 任何新增付费服务或提高预算。
- 真实云数据的删除、覆盖与灾难恢复演练。

## 三、需要你人工完成

这些事项通常涉及账号身份、验证码、实名、付款、平台协议或只有所有者掌握的业务决定，不能稳定地交给 agent 代办。

### 仍需人工完成

| 平台或事项 | 你今天需要做什么 | 完成后如何交接 |
| --- | --- | --- |
| Semantic Scholar Academic Graph API | 当前 key 已被服务端拒绝。官方没有公开的自助轮换控制台；先在[官方 API 页面](https://www.semanticscholar.org/product/api)通过 “Request an API Key” 重新提交个人研究用途申请。若希望恢复原 key，请联系 `feedback@semanticscholar.org`，提供申请邮箱、2026-04-10 申请日期、请求端点和 `403` 状态，切勿附上完整 key。 | 收到新 key 后更新忽略文件 `config/semantic_scholar.json`。只告诉 agent“文件已更新”，不要在聊天中发送 key。 |
| 历史凭证轮换 | Git 历史仍包含至少一个历史 OpenAI-style key pattern。当前强弱模型可用不代表旧凭证可以继续信任。 | 公开仓库或发布前轮换受影响凭证，并单独批准历史重写。 |

### 当前不需要申请

| 平台或事项 | 适用条件 | 你需要做什么 |
| --- | --- | --- |
| Apple Developer Program | 当前明确不向其他 Mac 用户分发 | 暂不申请；本地开发、测试和 `wails build` 不受影响。 |
| 发布身份 | 当前没有公开发布计划 | 暂不处理签名主体、notarization 和 App Store 资料。 |

Apple Developer 不是本地开发和 `wails build` 的前提。只有分发目标改变时才重新评估。

### 暂时不要申请

- 不需要为 arXiv 检索申请账号。
- 不要为常规 CI 配置真实 LLM key 或百度个人 token；CI 应使用 mock 和 fixture。
- 暂时不申请 Sentry 等遥测服务，先对齐隐私策略和是否真的需要收集崩溃信息。
- 暂时不新增云数据库、对象存储或账号系统；DiveEnd 当前是本地优先桌面工具。
- 不为检索接入未经允许的 Google Scholar 抓取服务。

## 四、凭证与本地文件交接规则

### 绝对不要做

- 不要把 API key、token、密码或证书粘贴到聊天、Markdown、Issue 或 PR。
- 不要把真实密钥写入 `.example` 文件。
- 不要把凭证放进前端代码、浏览器存储、截图或测试 fixture。
- 不要因为“只是私有仓库”而提交秘密。

### 正确交接方式

1. 从已有模板创建本地文件：

```bash
cp config/semantic_scholar.json.example config/semantic_scholar.json
cp config/strong_llm.json.example config/strong_llm.json
cp config/weak_llm.json.example config/weak_llm.json
cp baiduyun_token.json.example baiduyun_token.json
```

2. 你在本机编辑真实值。这四个目标文件已经被 `.gitignore` 排除。
3. 只回复“哪个文件已配置”，我会通过脱敏检查和最小请求验证，不显示秘密。
4. 每次 commit 或 push 前运行：

```bash
bash scripts/secret_scan.sh
git status --short --ignored
```

当前 Git 历史仍有历史秘密风险。公开仓库或正式发布前，必须先按 `docs/superpowers/plans/2026-06-11-secret-history-remediation.md` 轮换受影响凭证并执行经确认的历史重写。

## 五、获得前置条件后可持续自动执行的工作队列

这不是阶段计划，而是按工作类型维护的自治队列。每次选择其中价值最高且未被阻塞的一项。

### 核心产品

- 检索排序、去重、来源解释、失败降级和结果质量评测。
- DeepRead 长 PDF 加载、分页缓存、跨章节证据定位和引用跳转。
- 基于论文证据的问答、摘要、对比分析和可复用研究记录。
- 文库批量管理、筛选结果导入和阅读上下文连续性。

### 桌面可靠性

- PDF service 自动启动、健康检查、关闭、崩溃恢复和打包。
- Wails 进度事件、取消、恢复和窗口生命周期的真实应用验证。
- 本地数据库、managed files、迁移、备份和恢复的故障注入测试。
- macOS 窄窗口、高 DPI、键盘操作和大文件场景验证。

### 外部服务

- Semantic Scholar 与 arXiv 的限流、缓存、重试、去重和质量基准。
- 强弱模型路由、结构化输出校验、按任务统计成本和离线 fixture。
- 百度同步的授权刷新、冲突、断网恢复和真实账号 E2E。

### 工程与发布

- GitHub Actions 测试矩阵、依赖缓存、secret scan 和构建产物。
- 分支保护、PR 检查、发布环境和最小权限 Secrets。
- macOS 签名、notarization、版本化安装包和校验清单。
- 权威文档、项目地图、测试基线和发布说明同步更新。

## 六、你今天回复我的模板

不需要提供任何密钥。可以直接复制下面的内容并修改：

```md
### 功能设计
F1-F8：全部接受
修改项：

### 界面设计
U1-U8：全部接受
修改项：

### 自治权限
- 自动创建 codex/* 分支：允许 / 不允许
- 测试通过后自动 commit：允许 / 不允许
- 自动 push codex/*：允许 / 不允许
- 自动创建 PR：允许 / 不允许
- 使用本地凭证做有限真实 E2E：允许 / 不允许
- 单次和月度 API 预算上限：

### 外部前置
- Semantic Scholar：已申请 / 已配置 / 暂不申请
- LLM 强模型配置：已配置 / 待处理
- LLM 弱模型配置：已配置 / 待处理
- 百度网盘开放平台：已申请 / 已配置 / 不保留同步
- GitHub 管理权限与 2FA：已确认 / 待处理
- Apple Developer：已申请 / 暂不需要
- 发布主体与产品名：已确认 / 待设计
```

## 七、完成标准

长期自治开发的前置条件满足，意味着：

- 产品核心、短期界面方向和隐私边界已经明确。
- Agent 对分支、commit、push、PR、真实 E2E 的权限清楚。
- 必要凭证存在于本地忽略文件中，不需要通过聊天传递。
- 日常测试不依赖付费或个人账号，真实 E2E 有预算和数据边界。
- 高风险操作保留明确人工闸门。
- 每轮工作都能留下代码、测试证据、文档、提交和下一步，而不是只有对话结论。
