# 2026-06-10 Code Review Remediation

## Scope

This pass addresses the P0-P3 findings from the full project code review:

- Repository secret hygiene and local-only config tracking.
- Go quality gates, race safety, and LLM cancellation propagation.
- Sync reliability, database snapshotting, manifest-based upload, conflict actions, and persisted settings.
- Screening PDF ownership inside the DiveEnd data directory.
- PDF download hardening in the Go app and Python PDF service.
- Frontend dependency audit, DeepRead large-PDF rendering, and production chunking.
- Removal of unused legacy Go packages under `src/`.

## Completed Changes

- Removed tracked local files from git index while preserving local copies: `.DS_Store`, `auth.json`, `config.toml`, `test.txt`.
- Added ignore rules for local secrets and scratch files.
- Added `scripts/secret_scan.sh` to block tracked local secret files and high-confidence hard-coded secret patterns.
- Added `scripts/history_secret_audit.sh` and `docs/superpowers/plans/2026-06-11-secret-history-remediation.md` for non-destructive Git history secret auditing and an explicit cleanup/rotation plan. The current history audit is expected to fail until history rewrite is intentionally performed.
- Removed the tracked legacy Baidu manual test script and unused generated template (`baidupan_test.py`, `baidu_pcs.go.tmpl`), then added guardrails so similar scratch/token-refresh files cannot be committed again.
- Fixed the Go vet failure in `screening_workflow.go`.
- Added context-aware LLM calls for real `LLMClient` requests and routed DeepStart/DeepRead/Screening callers through cancellable helpers.
- Added context-aware PDF service calls and routed DeepStart/DeepRead/Screening PDF parse/extract paths through existing cancellation contexts.
- Serialized `SearchClient` progress callbacks so concurrent search providers cannot race reporter consumers.
- Reworked Sync upload to use a SQLite online-backup snapshot, `manifest.json`, stable remote keys, and a staging directory.
- Hardened Sync staging writes: the SQLite snapshot is now written through the sqlite3 online backup API into a pre-created fd-relative temp file, validated with `PRAGMA quick_check` on the destination connection, then reopened through the original directory fd for `0600` chmod + fsync before fd-relative commit; `manifest.json` uses the same atomic write helper as app config/token files.
- Hardened Sync staging directories: upload snapshot creation now treats `DataPath/.sync-staging` as a managed root, rejects a symlinked staging root before snapshot work starts, validates snapshot and manifest parent directories before writing, and performs cleanup only when the staging root is still a plain directory.
- Reworked Sync conflict resolution so `local` uploads the local file and `remote` downloads the cloud file. Active database replacement is staged and rejected with an explicit restart-safe restore message instead of silently overwriting the live DB.
- Added Sync progress locking, background manual sync, persisted Sync settings, startup sync opt-in, and best-effort shutdown sync opt-in.
- Added Wails `OnBeforeClose` handling for exit-time sync: users can sync then quit, quit directly, or cancel closing.
- Added exit-time Sync lifecycle regression tests covering terminal completion, error propagation, context cancellation, and the configured/auto-sync/database preconditions for prompting before close.
- Copied Screening uploads into `DataPath/screening/<session>/<paper>/` so imported papers no longer depend on external PDF paths.
- Hardened Screening cancellation cleanup: canceling a screening session now removes unreferenced managed PDFs under that session's `DataPath/screening/...` directory and cleans empty directories, while preserving files already imported into the main library because those library papers still reference the managed PDF path.
- Hardened Screening storage root handling: uploads now validate `DataPath/screening` as a managed root after source PDF validation but before target creation, and cancellation cleanup refuses to remove files/directories if the screening root or intermediate parents have been replaced by symlinks.
- Hardened local PDF copies used by manual DeepRead attachment and Screening upload import: sources and targets must be `.pdf`, sources must have a real `%PDF-` header, and managed copies are written through same-directory `0600` temp files with fsync + atomic rename.
- Hardened local PDF import size/type boundaries: manual DeepRead attachment, Screening upload import, and managed DeepRead path validation now reject empty, non-regular, and oversized PDFs, and local copy writes use the same bounded 100 MB reader as network PDF downloads.
- Hardened local PDF import source trust boundaries: manual DeepRead attachment and Screening upload import now reject symlinked PDF sources, validate the opened file is the same file observed during `Lstat`, copy from that same validated file handle, and revalidate the copied temp PDF before atomic rename so a source path/content race cannot inject non-PDF content into managed storage.
- Hardened managed PDF target directories: manual DeepRead attachment, background PDF downloads, and Sync remote-paper downloads now validate/create `DataPath/papers/...` parent directories one component at a time and reject existing symlink or non-directory components before staging a write. The lower-level local-copy and network-download writers also reject symlinked target directories before creating temp files or issuing HTTP requests, preventing corrupted local state from redirecting managed PDF writes outside the library tree.
- Hardened folder deletion cleanup: `DeleteFolderNode` now removes only lexical paths inside `DataPath/papers`, so corrupted `Paper.PDFPath` or folder IDs from old/local/synced data cannot delete arbitrary local files outside the managed library tree.
- Hardened managed deletion helpers: managed file/tree cleanup now rejects symlinked managed roots and symlinked intermediate parents and uses fd-relative cleanup for managed files/trees, preventing cleanup code from following a corrupted `DataPath/papers` or `DataPath/screening` parent into external directories.
- Hardened folder deletion failure semantics: `DeleteFolderNode` still skips external/out-of-root legacy PDF paths, but no longer ignores managed cleanup errors. If `DataPath/papers` or an intermediate managed parent is replaced with a symlink, deletion now fails before paper/folder rows are removed, preserving database/file consistency under local corruption.
- Hardened single-paper deletion cleanup: deleting a paper now removes its managed local PDF only when the file is inside `DataPath/papers` and no other paper references the same local file. External legacy paths are left untouched, shared managed PDFs are preserved, and symlinked managed roots abort deletion before the DB row is removed.
- Added a PaperList folder deletion entry point: non-system folders now expose a guarded delete action in the right-side library panel, ask for confirmation, remove descendant folders from frontend state, and switch back to a remaining system folder if the active folder was deleted.
- Added folder rename support end-to-end: non-system folders can be renamed from PaperList; the backend updates the target and descendant folder paths, moves the managed PDF directory, updates affected paper PDF paths and DeepRead parse cache paths, and rejects system-folder renames. The frontend refreshes folders and current papers after rename.
- Added folder move support end-to-end: non-system folders can be moved under another parent or back to the root; the backend rejects cycles and path conflicts, moves the managed PDF directory across parent directories with opened-directory `renameat`, updates descendant folder paths, and updates affected paper PDF paths and DeepRead parse cache paths transactionally.
- Added single-paper move support end-to-end: PaperList can move a paper to another folder; the backend rejects target-folder duplicates, copies managed PDFs into the target folder, updates the paper and DeepRead parse cache paths transactionally, preserves external PDFs, and preserves shared old managed PDFs for sibling papers.
- Added batch paper move support end-to-end: PaperList can select multiple visible papers and move them together; the backend performs duplicate/source and target-PDF collision preflight, copies each shared managed PDF only once, updates all selected papers and DeepRead cache paths in one transaction, cleans copied files on failure, and removes old managed PDFs only after confirming no remaining paper references them.
- Hardened Sync paper collection: upload snapshots and local conflict detection now include only validated managed PDFs under `DataPath/papers`; corrupted or external `Paper.PDFPath` values are skipped instead of being uploaded to cloud or considered for conflicts.
- Hardened Sync remote path trust boundaries: startup sync and conflict detection now ignore unsupported cloud keys, cloud downloads only accept `data/diveend.db` and managed paper PDF keys, `manifest.json` is never downloaded as local data, and conflict resolution revalidates remote key/local target pairs against current managed metadata before uploading or downloading.
- Hardened Sync remote paper downloads: paper files are downloaded into same-directory staging paths, validated as real `%PDF-` files, and only then atomically moved into the managed papers tree. Invalid cloud PDFs are cleaned up and cannot replace an existing valid local PDF during conflict resolution.
- Fixed Sync conflict path comparison on macOS-style symlinked temp paths by comparing existing local paths after symlink resolution, avoiding false metadata mismatch errors for equivalent `/var` and `/private/var` paths.
- Changed Baidu token saves to `0600`, preserved refresh/client credentials independently, and changed Baidu upload to stream chunks instead of holding all chunks in memory.
- Hardened shared atomic writes and Baidu downloads: `writeFileAtomic` and `BaiduPCSClient.DownloadFile` now reject symlinked target/output directories before creating temp files or issuing cloud requests, preventing config/token/manifest/download writes from being redirected through a corrupted local directory.
- Hardened shared local directory creation: `ensurePlainDirectory` now creates/checks directories component-by-component and rejects intermediate symlink components, while preserving macOS system `/var` and `/tmp` compatibility aliases; SQLite `NewDB` now uses this path too, so a symlinked `DataPath` cannot redirect database creation.
- Finished production directory-creation hardening: remaining database restore and managed library root creation paths now route through the same component-by-component checks, so `.sync-restore`, restore backup parents, and `DataPath/papers` parents reject symlinked `DataPath` or intermediate folder components before any DB/PDF write.
- Hardened atomic file replacement against directory-swap races: config/token/manifest writes, local PDF copies, Go PDF downloads, Baidu downloads, database restore copies, and Sync paper PDF final moves now create temp files and rename them relative to an opened no-follow directory file descriptor via `openat`/`renameat`, verify the directory path still references the same inode before and after replacement, and clean up through `unlinkat`. Shared single-file and empty-directory cleanup also uses fd-relative `unlinkat`.
- Hardened managed recursive cleanup: folder deletion and Sync staging cleanup no longer use path-following `RemoveAll`; managed trees are removed entry-by-entry through opened no-follow directory file descriptors, symlinks inside the tree are unlinked without following their targets, and directory entries are checked for inode stability before removal.
- Removed the SQLite `VACUUM INTO` snapshot surface: Sync database snapshots now use `github.com/mattn/go-sqlite3`'s online backup API against an already-created secure temp file, validate through the destination connection, reopen/sync the temp file through the original directory fd, and commit through the fd-relative atomic rename helper.
- Hardened Baidu remote path handling: upload, directory creation, listing, recursive listing, file info, download, and delete operations now reject remote paths that escape `/apps/<syncApp>/` before any cloud request.
- Hardened Baidu remote listing trust boundaries: list responses now skip returned file/directory entries that are outside `/apps/<syncApp>/` or whose paths change after normalization, preventing unexpected cloud metadata from driving recursive sync outside the app root.
- Hardened Baidu destructive path handling: delete operations now refuse to delete the `/apps/<syncApp>` application root itself, and list metadata parsing accepts numeric fields returned as JSON numbers or strings without dropping size/mtime/isdir data.
- Hardened Baidu upload handling: uploads now reject symlinked/non-regular/empty/check-open-swapped local sources before any cloud request, reuse the same opened file for block hashing and chunk upload, fail if the source changes mid-upload, and treat server chunk MD5 mismatches as hard upload failures instead of log-only warnings.
- Added PDF URL SSRF guards and a 100 MB download limit in the Go downloader.
- Hardened Go PDF URL redirects: `downloadPDFToFile` now validates every redirect target before following it and rejects redirects into localhost/private networks.
- Hardened Go-managed PDF downloads: `downloadPDFToFile` now writes through same-directory `0600` temp files, validates PDF content before replacing the target, fsyncs successful downloads, and preserves existing files on truncated/failed responses.
- Hardened Go-managed PDF download error disclosure: signed/manual PDF URL request-creation and network errors now redact URL query values before returning, preventing tokenized PDF links from leaking through download failures.
- Optimized Go-to-Python PDF parsing uploads: `PDFServiceClient.ParsePDFWithContext` now streams multipart upload bodies with `io.Pipe` instead of buffering the full PDF in memory before sending it to the local PDF service.
- Hardened Go-to-Python PDF parsing uploads: the Go PDF service client now rejects non-`.pdf`, symlinked, non-regular, empty, fake-header, and oversized files before issuing a request, and the multipart stream is capped at the same 50 MiB default as the Python PDF service.
- Hardened Python PDF service defaults: `127.0.0.1` bind, restricted CORS regex, upload size limits, streaming URL downloads, private-host rejection, temp-file cleanup.
- Hardened PDF service container exposure: Docker Compose now publishes port `50051` on host loopback only, while direct local-run documentation uses `127.0.0.1` instead of `0.0.0.0`.
- Hardened PDF service startup guidance: Go client connection-failure errors now suggest `uvicorn ... --host 127.0.0.1` and have regression coverage ensuring the user-facing hint does not drift back to `0.0.0.0`.
- Hardened Python PDF URL redirects: `/parse/url` now disables automatic redirects and manually validates each redirect target before following it, preventing a public URL from bouncing the service into localhost/private networks.
- Hardened Python PDF service temp files: upload and URL parse routes now create `0600` temporary PDFs, flush + fsync them before parsing, and retain the existing finally cleanup behavior.
- Hardened Python PDF content validation: `/parse/upload` and `/parse/url` now require a real `%PDF-` magic header before invoking the parser, so fake PDFs with `.pdf` names or `application/pdf` content type are rejected with 400 instead of consuming parser resources or surfacing as parser failures.
- Hardened Python PDF service log/error disclosure: FastAPI global HTTPException details now pass through recursive credential redaction before logging and response serialization; generic unhandled exceptions and low-level LLM chat failures log only exception types; uploaded PDF parser failures now return/log generic safe errors instead of reflecting parser exception strings.
- Hardened Python extraction partial-parse logging: metadata/metrics schema or JSON parse failures now log only exception types instead of raw exception strings, preventing malformed LLM responses or Pydantic validation errors from writing credential-bearing output snippets into logs.
- Hardened Python parser-unavailable disclosure: PyMuPDF4LLM runtime import failures now log only exception types and return a generic parser-unavailable error, avoiding local path/environment detail leakage when parser dependencies are missing.
- Upgraded `react-router-dom` to a production-audit-clean version.
- Added Vite `manualChunks`; main frontend bundle is now about 195 KiB instead of about 817 KiB.
- Added DeepRead PDF page-window rendering to avoid rendering every PDF page canvas at once.
- Stopped returning full API keys/tokens from the settings prefill API; frontend now receives only configured/not-configured flags.
- Deleted unused legacy packages under `src/config`, `src/database`, and `src/llm`.
- Added a restart-safe cloud database restore wizard: remote DB conflicts are downloaded to staging, validated, shown in Sync UI, and can be applied with local backup + DB reload. Pending restore markers also apply on next startup.
- Added an asset-server backed DeepRead PDF URL path (`/deepread/pdf/<token>`) so the frontend can load PDFs through same-origin range-friendly HTTP instead of eagerly base64-loading the full file. The old base64 method remains as fallback.
- Fixed the DeepRead frontend PDF fallback path: if the asset URL bridge is missing or temporarily fails, the reader now still attempts the backend byte fallback instead of silently rendering an empty PDF area.
- Hardened DeepRead local PDF access: prepare, base64 fallback, generated asset URLs, and the asset server now require a real `%PDF-` file under the current `DataPath/papers` tree after symlink resolution, preventing stale/synced `PDFPath` values from exposing arbitrary local files.
- Bounded the DeepRead base64 PDF fallback: `GetDeepReadPDFBytes` now rejects PDFs above the fallback size cap and forces large PDFs through the range-friendly asset URL path, preventing accidental full-file base64 memory spikes.
- Hardened DeepRead base64 fallback reads: `GetDeepReadPDFBytes` now uses the shared limited reader and revalidates the `%PDF-` header after reading, so a managed PDF cannot grow past the fallback cap between `stat` and `read`.
- Added `sync-progress` Wails events and frontend subscription, with polling kept as fallback.
- Added Wails `ts_type:"string"` mappings for exported `time.Time` fields so generated frontend models use `string` instead of `any` and the binding generator no longer emits `time.Time` warnings.
- Replaced remaining runtime `fmt.Println` sync/token status logs with standard logger calls.
- Added BaiduPCS mock protocol tests covering token validity checks, expired-token refresh and secure token persistence, remote directory creation, chunked upload, recursive listing, and download without touching real Baidu Cloud.
- Added Sync progress source tests covering `TriggerSync` background execution, progress reporter snapshots, concurrent trigger behavior, and immutable progress polling.
- Normalized frontend Sync progress completion status to `complete` while remaining compatible with legacy `completed` payloads.
- Hardened Sync active/terminal status handling in both Go and the Sync page so legacy `completed` progress snapshots are treated as terminal and trigger a UI refresh instead of leaving the page in a false in-progress state.
- Hardened Sync settings UX: checkbox changes are still optimistic while saving, but failed saves now roll back to the last confirmed settings, show the backend error, and temporarily disable settings toggles during the save request so rejected config changes do not look persisted.
- Added Sync page event-driven tests covering initial backend state rendering, `sync-progress` UI updates, completion-triggered refresh, and event unsubscription on unmount.
- Unified Wails event emission behind an injectable `App.emitEvent` wrapper and added backend coverage proving Sync progress snapshots are emitted as `sync-progress` runtime events with the expected payload before they reach the desktop bridge.
- Added an opt-in real Baidu Cloud E2E test (`TestRealBaiduSyncE2E`). It is skipped by default, only runs when `DIVEEND_REAL_BAIDU_E2E=1` is set, uses an isolated `diveend-e2e-*` remote root, uploads a temporary SQLite DB snapshot plus a temporary PDF, validates the remote listing and manifest download, and attempts remote cleanup.
- Fixed Baidu download metadata handling observed during real E2E: `DownloadFile` now preserves `fs_id` with `json.Decoder.UseNumber()` and sends `filemetas` `fsids` as a numeric array instead of a string array.
- Hardened Baidu cloud downloads: `DownloadFile` now writes into a same-directory `0600` temp file, fsyncs it, atomically renames on success, syncs the parent directory, and preserves any existing target file if the remote response is truncated or the download fails.
- Hardened Baidu cloud download links: `DownloadFile` now validates `filemetas` `dlink` values against Baidu/BaiduPCS host boundaries, rejects literal localhost/private-IP links and redirects, keeps the download User-Agent across redirects, and appends `access_token` by merging query parameters instead of string-concatenating a second `?`.
- Hardened Baidu network error disclosure: all Baidu token-check, refresh, upload, directory-create, list, metadata, download, and delete request-construction/network errors now redact credential-bearing URL query parameters before returning/logging errors, so `access_token`, `refresh_token`, and `client_secret` cannot leak through `http.Client` URL errors.
- Hardened Baidu decoded failure disclosure: decoded API failure payloads and upload failure `show_msg` logs now pass through shared credential redaction before errors/logs are returned, so server-side JSON echo of `access_token`, `refresh_token`, `client_secret`, API keys, or bearer tokens cannot leak through list/create/delete/precreate/create failures.
- Fixed Sync false-positive completion: `SyncToCloud` now continues attempting remaining uploads, records failed uploads, returns an error when any upload fails, and leaves progress in `error` instead of overwriting it with `complete`.
- Added Sync upload token preflight: `SyncToCloud` now validates/refreshes the Baidu access token before creating local snapshots or starting per-file uploads, so an expired/invalid refresh token fails once with an actionable credential error instead of repeating the same failure for every file.
- Fixed startup Sync false-positive completion: `SyncOnStartup` now continues attempting remaining downloads, records failed downloads, returns an error when any download fails, and leaves progress in `error` instead of overwriting it with `complete`.
- Fixed startup Sync progress consistency: unsupported cloud files and directories are excluded from progress totals, so completed startup sync no longer reports misleading partial counts such as `0/1` after safely skipping unknown remote keys.
- Fixed Sync pending-file semantics: Sync status and exit-before-sync prompts now count only local files that have no successful upload record or were modified after their latest successful upload, instead of treating every managed local file as pending forever.
- Updated real Baidu E2E token persistence so successful access-token refreshes are written back to the configured token file; this avoids consuming a rotated refresh token and discarding its replacement.
- Hardened real Baidu E2E setup: the opt-in live test now preflights the Baidu token before registering remote cleanup or starting sync, so invalid credentials fail with a clear precondition error and do not trigger extra cleanup-side refresh attempts.
- Serialized Baidu token refreshes inside `BaiduPCSClient` so concurrent sync/status/conflict calls cannot consume a rotating refresh token multiple times.
- Hardened direct Baidu token loading: `LoadBaiduToken` now rejects empty paths, symlinks, non-regular files, and token files larger than the seed config cap instead of reading arbitrary token paths unboundedly.
- Serialized Sync cloud operations (`SyncToCloud`, `SyncOnStartup`, `DetectConflicts`, and `ResolveConflict`) with the same manager mutex so conflict checks cannot interleave cloud listing/downloads with an in-progress upload.
- Kept the Sync page responsive during active uploads: `GetSyncStatus` and `GetSyncConflicts` now return cached local conflict data while sync is in progress instead of blocking on a serialized cloud conflict scan.
- Fixed restart-required DataPath changes: the new DataPath is persisted and returned to settings UI/`GetInitialState`, but the running app keeps the old DataPath/DB/SyncManager until restart. Subsequent Sync settings saves preserve the pending DataPath instead of overwriting the config file with the runtime path.
- Fixed frontend Wails event cleanup: progress subscribers now call only the unsubscribe function returned by `EventsOn`, instead of global `EventsOff(event)` that could remove other active listeners for the same event.
- Fixed background PDF download path drift: queued download jobs now carry the DataPath snapshot from enqueue time, so a later config save cannot cause already queued downloads to write into a different data directory.
- Hardened paper import folder targets: plain import, asset import, and Screening completion now reject unknown target folder IDs instead of creating orphaned paper rows, and the PDF download worker sanitizes corrupted queued folder IDs plus verifies final targets remain under `DataPath/papers`.
- Hardened paper PDF filenames: local PDF attachment, background PDF download jobs, and Sync remote paper keys now derive PDF filenames from sanitized paper ID segments, so corrupted paper IDs cannot become path separators in local filenames or cloud keys.
- Hardened local storage overview counts: stored PDF file counts now include only regular files that pass the same `%PDF-` header validation used by DeepRead, so fake `.pdf` files and symlinked PDFs do not inflate dashboard storage statistics.
- Fixed config hot-update sync safety: `SaveConfig` no longer reloads the SQLite DB or replaces `SyncManager` for unrelated settings changes, and sync-relevant Baidu/runtime DataPath changes are rejected while cloud sync is actively running.
- Fixed close/shutdown sync duplication: exit-time sync now uses a non-blocking `SyncToCloudIfIdle` path and waits for an already active sync to finish instead of queueing a second upload pass behind it.
- Fixed shutdown Sync over-triggering: the best-effort shutdown upload now reuses the same pending-local-upload check as the close prompt, so closing the app with `autoSync` enabled no longer makes a hidden Baidu request when all local candidates already have successful upload records. The Sync page copy now explains that the exit-time setting only prompts/runs when local uploads are pending.
- Connected the previously dormant `SyncSettings.SyncInterval` setting to real periodic background sync: when `autoSync` is enabled and Baidu sync is configured, the app starts a cancellable interval loop that checks for pending local uploads and only calls cloud sync when local candidates are newer than the latest successful upload records. Saving Sync settings starts/stops this loop immediately, and shutdown stops it before closing the database. The Sync page now exposes interval selection instead of hiding the persisted field.
- Connected the previously dormant `SyncSettings.ConflictResolution` setting to conflict refresh: `manual` preserves unresolved conflicts for the Sync UI, while `timestamp`, `local`, and `remote` attempt automatic conflict resolution during refresh and keep only failed/unresolved conflicts visible. Database remote-wins resolution still uses the restart-safe pending-restore staging flow instead of replacing the live database. The Sync page now exposes the conflict strategy selector.
- Added Sync history records for conflict resolution actions: local-wins conflicts save successful `upload` records, remote-wins conflicts save `download` records, and failed conflict operations save failed records. Database upload pending detection now ignores the tiny SQLite mtime bump caused by writing the successful sync record itself, preventing periodic auto-sync from immediately re-uploading the database solely because sync history was recorded.
- Added user-readable Sync history messages: `sync_records` now has a migrated `message` column, regular upload/download records describe the operation, conflict resolution records explain whether local or cloud won, and the Sync history UI displays this message with sanitized fallback labels for old records.
- Added legacy `sync_records` migration coverage for the new `message` column so existing local databases without that column can upgrade in place, keep old records readable, and round-trip new human-readable history messages.
- Added Sync conflict diff metadata and overwrite preservation: conflict detection stores file kind, local/remote sizes, and newer side; the Sync UI shows those details before resolution; local-wins archives the cloud version and remote-wins archives the local version under `DataPath/.sync-conflicts/` before any overwrite or pending database restore.
- Added legacy `sync_conflicts` migration coverage for the new diff metadata columns so existing local databases can upgrade in place, keep old unresolved conflicts readable, and round-trip new conflict metadata.
- Made Sync conflict refresh idempotent: unchanged unresolved conflicts are no longer deleted and reinserted on every status refresh, preserving conflict IDs and avoiding unnecessary writes to the live SQLite database. For live database conflicts, identity comparison ignores local DB mtime changes caused by Sync's own bookkeeping writes and keys the conflict on the stable local/remote paths plus remote mtime.
- Fixed resolved-conflict visibility after idempotent refresh: Sync conflict APIs now return only unresolved conflicts from cached, in-progress, unavailable-cloud, and idempotent refresh paths, so a resolved row retained for internal history does not continue appearing in the UI conflict list or count.
- Finalized database upload baselines at the end of successful cloud-upload batches: when `data/diveend.db` is uploaded before paper assets and `manifest.json`, the original database upload record's `completed_at` is updated after the full batch succeeds. This prevents later sync-history writes in the same batch from making the live SQLite file immediately appear pending again, without adding duplicate database upload rows.
- Split periodic Sync cancellation into UI-safe and lifecycle-safe paths: saving Sync settings now cancels the existing periodic schedule without waiting for a slow in-flight worker to finish, so the settings UI does not hang on cloud latency. Shutdown and DB-reload paths still wait for periodic workers before closing/replacing the database, preserving safety.
- Serialized periodic Sync worker stop/start lifecycle operations with a dedicated mutex so a lifecycle `Wait()` cannot overlap with another settings save adding a new worker to the same wait group. This preserves the non-blocking settings-save behavior while avoiding unsafe `WaitGroup` Add/Wait interleavings under concurrent settings save and shutdown/reconfigure calls.
- Split exit-time Sync prompting from periodic auto-sync: `syncBeforeExit` now controls close/shutdown prompts independently from `autoSync`, while periodic background upload checks remain tied to `autoSync`. The Sync page exposes this as a separate "Sync before exit" toggle and the Settings panel copy no longer describes the feature as pending.
- Fixed manual Sync rapid-trigger queuing: `TriggerSync` now reserves the cloud-sync slot synchronously through `StartSyncToCloudAsync`, so a fast second click returns the current in-progress snapshot instead of launching a second queued upload pass.
- Fixed sensitive config/token write durability: app config and Baidu token saves now write to a same-directory `0600` temp file, fsync it, atomically rename it into place, and best-effort fsync the parent directory, avoiding partial/truncated secret files on interrupted writes.
- Hardened config and secret-seed loading: app config reads are capped at 1 MiB, legacy seed config/token/key files are capped at 256 KiB, and non-regular files plus symlinks are rejected so misplaced huge files or link targets cannot be read unboundedly at startup.
- Hardened limited local file reads against check/open races: config, seed, and direct Baidu token reads now verify that the opened file is the same file observed during `Lstat`, rejecting paths that change between validation and read.
- Hardened database restore persistence: pending restore markers now use atomic `0600` writes, and backup/replacement database copies use random same-directory temp files with fsync + atomic rename + parent directory sync. Tests now assert marker/DB/backup permissions, absence of restore temp leftovers, and target preservation when a copy cannot start.
- Hardened database restore copy targets: `copyFileAtomic` now rejects symlinked target directories before creating temp files, adding a lower-level guard beneath the existing restore marker path validation.
- Hardened database restore marker trust boundaries: remote DB conflict staging now lands in `DataPath/.sync-restore`, and pending restore markers are rejected if their staged database or backup path escapes that restore directory lexically or through symlink resolution.
- Hardened database restore control files: pending restore markers are now read through a 64 KiB limited reader that rejects symlinked/non-regular markers, and `.sync-restore` itself is rejected if it has been replaced with a symlink before marker read/write/remove operations.
- Hardened database restore marker cleanup: applying or canceling a pending restore now removes `pending.json` through a parent-directory fsync helper, and apply reports marker cleanup failures instead of silently leaving a repeat-restore marker behind.
- Hardened remote database conflict staging: resolving a DB conflict with the remote version now creates `DataPath/.sync-restore` with `0700` permissions before download, rejects a symlinked restore directory before any Baidu request, and then schedules the validated pending restore marker.
- Reduced default Baidu/Sync log sensitivity: upload/download logs no longer print full local paths, full remote app paths, upload IDs, fs IDs, or chunk MD5 values. A regression test now captures upload logs and rejects those sensitive values.
- Hardened Baidu refresh HTTP defaults: standalone token refresh and nil-client refresh paths now use the same 30 second timeout as `BaiduPCSClient`, instead of falling back to `http.DefaultClient` without a timeout.
- Reduced startup restore log sensitivity: applying a pending cloud database restore no longer prints the full local backup path, with regression coverage ensuring `BackupPath` stays out of startup logs.
- Hardened DeepStart markdown cache persistence: parsed markdown is now saved through the shared atomic `0600` write helper, and backend coverage verifies cache content, permissions, and absence of temp-file leftovers.
- Hardened DeepStart preprocessing cache paths: session IDs are sanitized before building `DataPath/deepstart_cache/...` PDF and markdown cache directories, so corrupted or synced session records cannot create cache files outside the intended cache tree.
- Hardened DeepStart preprocessing cache directories: `DataPath/deepstart_cache` is now treated as a managed root, and PDF/markdown cache targets are validated and created one component at a time before download or markdown persistence. A symlinked cache root is rejected before any PDF download starts, preventing preprocessing artifacts from being redirected outside `DataPath`.
- Removed the unused legacy DeepStart `SelectionPanel` barrel export/component, eliminating a stale TODO and hard-coded mock Inbox selector from the maintained frontend surface.
- Hardened external paper links in DeepStart and DeepRead by using explicit `rel="noopener noreferrer"` on every `_blank` anchor.
- Hardened frontend backend fallback boundaries for critical Screening/Sync/database-restore APIs: browser preview can still use mocks, but a partial Wails App binding now fails loudly instead of silently returning mock/default results.
- Hardened config redaction coverage: Go tests now assert legacy secret fields and Baidu refresh/client credentials are stripped from sanitized config, and frontend mock `saveConfig` now redacts Baidu refresh/client credentials as well as the access token.
- Removed the unused `arxiv-sanity-lite` search implementation that used a plaintext HTTP endpoint and was no longer part of the active two-source search workflow; `EnhancedSearch` now reports only the active Semantic Scholar and arXiv sources.
- Hardened Python `/parse/url` logging and error responses: signed PDF URLs are logged by host only, query strings are not written to logs, and failed downloads return generic errors instead of reflecting tokenized URLs.
- Hardened Python `/parse/upload` filename handling: uploaded PDF extensions are validated case-insensitively, and upload logs use basename-only filenames so client-side path fragments are not logged.
- Hardened PDF extraction LLM configuration handling: Go now rejects missing extraction API keys before calling Python `/extract/` with an actionable Settings/Credentials error, and Python LLM config treats blank keys as missing so direct service calls can still fall back to environment variables.
- Hardened PDF extraction request boundaries: Go now rejects empty or oversized markdown before calling Python `/extract/`, and the Python extraction route enforces markdown length plus bounded `max_tokens`, `temperature`, and `timeout` values at request validation time.
- Hardened PDF extraction error disclosure: Python `/extract/` now returns a client-safe generic error for upstream LLM failures and logs only the exception type, while preserving explicit configuration errors such as missing API keys.
- Hardened PDF extraction config-error echoing: preserved `/extract/` configuration errors and the error response `provider` field now pass through credential redaction, so invalid provider strings cannot echo embedded API keys or tokens back to the frontend.
- Hardened frontend Settings credential handling: secret prefill values are discarded even if a runtime bridge returns them, Settings only uses presence flags, and async secret prefill no longer resets unrelated unsaved draft changes such as DataPath edits while the user types an API key.
- Removed frontend dev/mock secret seed loading: browser mock mode no longer fetches `config/strong_llm.json`, `config/weak_llm.json`, or `baiduyun_token.json` through Vite `/@fs`, and the frontend bundle no longer receives the local project root path.
- Hardened frontend user-visible error handling: global app errors, Sync/Screening page errors, backend-normalized paper/PDF/search/progress/restore/Sync-history errors now pass through a shared redaction helper that hides API keys, bearer credentials, query tokens, client secrets, and oversized error bodies before they reach the UI.
- Hardened DeepStart local runtime messages: chat action failures and Wails `deepstart-progress` messages now pass through the same frontend redaction helper before rendering in the sticky chat/progress UI, covering local component state paths that do not go through the global app-store error setter.
- Hardened remaining frontend local error state: Settings save status, DeepRead translation failure text, and DeepStart create-folder modal errors now use the shared redaction helper before rendering, closing local state paths that bypass the global store error setter.
- Hardened the remaining frontend local error display paths: App initialization, DeepRead library/state/prepare/note/theme/download/manual-PDF actions, DeepStart start/cancel/session refresh/selection/import/undo actions, and detail-drawer clipboard copy failures now use the shared safe error formatter before showing messages.
- Hardened PaperList error handling consistency: folder-load, folder-create, and paper-delete failures now use the shared user-visible error formatter before entering global error state, with component tests covering API key/token redaction for create/delete failures.
- Fixed Screening file input/drop path-resolution failures so bridge errors are caught, displayed safely, and do not leave unhandled promises. The browser-preview demo button is now disabled while work is in progress and the empty-selection copy no longer mentions raw mock paths.
- Added DeepStart detail-drawer clipboard feedback: copying abstracts now shows success/failure state, gracefully handles missing clipboard support, and redacts sensitive values from clipboard API failures.
- Added bounded HTTP response body reads for external LLM/Search/DeepStart enrichment responses and PDF-service error responses, preventing oversized upstream bodies from being read unboundedly into memory.
- Extended bounded response handling to JSON decoders: Baidu Cloud JSON responses now decode through the same external response limit, PDF service success responses use a bounded local-service JSON limit, and Baidu fs_id decoding keeps `UseNumber` without unbounded reads.
- Hardened Baidu cloud downloads: remote list metadata size is now enforced before requesting the download body, the streamed body is capped, completed downloads must match the remote declared size, and failed/truncated downloads continue preserving any existing target file.
- Hardened Go external error disclosure: LLM provider error bodies, Semantic Scholar/arXiv provider error bodies, PDF service HTTP error bodies, PDF service `success=false` errors, and PDF service connection hints now pass through shared credential redaction and user-visible length bounding before being returned or logged.
- Hardened Search retry/progress error privacy: search retry logs, `search-progress` source errors, and final aggregated search failures now redact credential-like values and URL query values before leaving `SearchClient`, preventing provider/network errors from exposing API tokens or private search query text through logs or frontend progress events.
- Hardened LLM client network/config error privacy: provider labels, unknown wire API errors, and responses/chat-completions/Anthropic request creation or network failures now redact credential-like values plus URL query values before returning errors, preventing misconfigured provider names/base URLs or transport errors from echoing API keys, bearer tokens, or private query strings.
- Hardened DeepStart enrichment network error privacy: OpenAlex/Crossref metadata request creation and network failures now redact URL query values before returning errors, preventing private title/query tokens from leaking through enrichment failure paths.

## Verification

- `go test ./...` passed.
- `go test ./... -run 'TestConfigureSyncManagerEmitsRuntimeSyncProgress|TestTriggerSyncPublishesProgressSnapshots|TestSyncProgress'` passed.
- `go test ./... -run 'TestSyncToCloudReturnsErrorWhenUploadsFail|TestTriggerSyncPublishesProgressSnapshots|TestBaiduPCSClientUploadListAndDownloadContracts|TestRealBaiduSyncE2E' -v` passed locally, with the real Baidu test taking its default skip path.
- `go test ./... -run 'TestSyncOnStartupReturnsErrorWhenDownloadsFail|TestSyncToCloudReturnsErrorWhenUploadsFail' -v` passed.
- `go test ./... -run 'TestDetectConflictsWaitsForInProgressSync|TestBaiduPCSClientSerializesConcurrentTokenRefresh|TestSyncToCloudReturnsErrorWhenUploadsFail' -v` passed.
- `go test ./... -run 'TestSyncAPIsReturnCachedConflictsDuringInProgressSync|TestDetectConflictsWaitsForInProgressSync' -v` passed.
- `go test ./... -run 'TestAppSaveConfigPersistsAndSignalsRestart|TestSyncAPIsReturnCachedConflictsDuringInProgressSync' -v` passed.
- `go test ./... -run 'TestDownloadJobUsesQueuedDataPathSnapshot|TestAppImportPapersWithAssetsDownloadsPDFAndUpdatesStorageOverview|TestRetryPaperDownload' -v` passed.
- `go test ./... -run 'TestAppSaveConfig(PreservesActiveSyncManagerForUnrelatedChanges|RejectsSyncConfigChangesDuringActiveSync|PersistsAndSignalsRestart)' -v` passed.
- `go test ./... -run 'TestSyncToCloudIfIdleDoesNotQueueBehindActiveSync|TestWaitForSyncCompletionReturnsAfterActiveSyncCompletes|TestTriggerSyncPublishesProgressSnapshots' -v` passed.
- `go test ./... -run 'TestStartSyncToCloudAsyncReservesSyncSlotImmediately|TestTriggerSyncPublishesProgressSnapshots|TestSyncToCloudIfIdleDoesNotQueueBehindActiveSync' -v` passed.
- `go test ./... -run 'TestSaveAndLoadAppConfigRoundTrip|TestBaiduPCSClientRefreshesExpiredTokenAndPersistsSecurely' -v` passed.
- `go test ./... -run 'TestDatabaseRestoreScheduleAndApply|TestCopyFileAtomicFailurePreservesTarget' -v` passed.
- `go test ./... -run 'TestDatabaseRestore|TestScheduleDatabaseRestore|TestPendingDatabaseRestore|TestCopyFileAtomicFailurePreservesTarget' -v` passed.
- `go test ./... -run 'TestDatabaseRestore|TestCancelPendingDatabaseRestore|TestScheduleDatabaseRestore|TestPendingDatabaseRestore|TestCopyFileAtomicFailurePreservesTarget' -v` passed.
- `go test ./... -run 'TestDatabaseRestore|TestPendingDatabaseRestore|TestCancelPendingDatabaseRestore|TestScheduleDatabaseRestore|TestCopyFileAtomicFailurePreservesTarget' -v` passed after pending marker size/symlink and restore-directory symlink hardening.
- `go test ./... -run 'Test(DatabaseRestore|ScheduleDatabaseRestore|PendingDatabaseRestore|CancelPendingDatabaseRestore|StartupAppliesPendingDatabaseRestore|ResolveConflictRemoteDatabase)' -v` passed after remote database conflict staging directory hardening.
- `go test ./... -run TestStartupAppliesPendingDatabaseRestoreWithoutLoggingBackupPath -v` passed.
- `go test ./... -run TestPreprocessDeepStartResultsWritesMarkdownCacheAtomically -v` passed.
- `go test ./... -run 'TestSearchClientSearchRequestsAtLeast100FromSemanticAndArxiv|TestEnhancedSearchReportsOnlyActiveSources|TestSearchClientUsesPerSourceLimitWhenOverallLimitIs200' -v` passed.
- `go test ./... -run 'TestGetDeepReadStateRepairsStaleMissingPDFCache|TestDeepRead(ManagedPDFURLAndBytes|RejectsPDFOutsideManagedPapersDirectory|RejectsManagedSymlinkEscape)' -v` passed.
- `go test ./... -run 'TestDeepRead(ManagedPDFURLAndBytes|PDFBytesRejectsLargeFallbackButAssetURLStillWorks|RejectsPDFOutsideManagedPapersDirectory|RejectsManagedSymlinkEscape)' -v` passed.
- `go test ./... -run 'TestDeepRead(ManagedPDFURLAndBytes|PDFBytesRejectsLargeFallbackButAssetURLStillWorks|RejectsPDFOutsideManagedPapersDirectory|RejectsManagedSymlinkEscape)|TestReadLimitedFileRejectsFileSwapDuringOpen' -v` passed after bounded DeepRead fallback read hardening.
- `go test ./... -run 'TestAppScreeningFlowImportsSelectedPapers|TestAttachLocalPDFToPaper(CopiesValidPDFSecurely|RejectsFakePDFContent)' -v` passed.
- `go test ./... -run 'TestCancelScreening(RemovesManagedSessionFiles|PreservesLibraryReferencedFiles)' -v` passed.
- `go test ./... -run 'TestDeleteFolderNodeDoesNotRemove(ExternalPDFPath|PathEscapedByCorruptFolderID)' -v` passed.
- `go test ./... -run 'TestBaiduPCSClient(UploadListAndDownloadContracts|DownloadFailurePreservesExistingFile)' -v` passed.
- `go test ./... -run 'TestBaiduPCSClientUpload(ListAndDownloadContracts|RejectsRemotePathEscapesBeforeRequest|RejectsUnsafeLocalSourcesBeforeRequest|RejectsChunkMD5Mismatch)|TestOpenBaiduUploadSourceRejectsFileSwapDuringOpen' -v` passed.
- `go test ./... -run 'TestBaiduPCSClient(UploadListAndDownloadContracts|RejectsRemotePathEscapesAcrossOperations|UploadRejectsRemotePathEscapesBeforeRequest)|TestRealBaiduSyncE2E' -v` passed with the real Baidu test taking its default skip path.
- `go test ./... -run 'TestBaiduPCSClient(RejectsDeletingAppRootBeforeRequest|ListFilesSkipsUnexpectedRemotePaths|UploadListAndDownloadContracts|RejectsRemotePathEscapesAcrossOperations)' -v` passed.
- `go test ./... -run 'TestBaiduPCSClient' -v` passed, including log privacy regression coverage.
- `go test ./... -run 'TestBaidu(DownloadURLWithAccessTokenPreservesExistingQuery|PCSClientRejectsUnexpectedDownloadLinkHost|PCSClientRejectsDownloadRedirectToPrivateHost|PCSClientUploadListAndDownloadContracts|PCSClientDownloadFailurePreservesExistingFile|PCSClientDownloadSilentTruncationPreservesExistingFile)' -v` passed.
- `go test ./... -run 'TestBaiduPCSDefaultHTTPClientsUseTimeout|TestBaiduPCSClientRefreshesExpiredTokenAndPersistsSecurely|TestBaiduPCSClientSerializesConcurrentTokenRefresh' -v` passed.
- `go test ./... -run 'TestLoadBaiduTokenRejectsUnsafeFiles|TestBaiduPCSClientRefreshesExpiredTokenAndPersistsSecurely|TestBaiduPCSClientSerializesConcurrentTokenRefresh' -v` passed.
- `go test ./... -run 'TestPrepareSyncSnapshotWritesSecureAtomicFiles|TestTriggerSyncPublishesProgressSnapshots' -v` passed.
- `go test ./... -run 'TestPrepareSyncSnapshot(WritesSecureAtomicFiles|SkipsExternalPaperPDFPaths)' -v` passed.
- `go test ./... -run 'TestDownload(JobUsesQueuedDataPathSnapshot|PDFToFileFailurePreservesExistingTarget)|TestAppImportPapersWithAssetsDownloadsPDFAndUpdatesStorageOverview' -v` passed.
- `go test ./... -run 'TestAppImportsRejectUnknownTargetFolder|TestProcessDownloadJobSanitizesCorruptFolderIDFallback|TestAppImportPapersWithAssetsAllowsFolderCopiesAndSkipsSameFolderDuplicates|TestAppImportPapersWithAssetsDownloadsPDFAndUpdatesStorageOverview|TestAppScreeningFlowImportsSelectedPapers|TestAppImportAndTranslateFlow' -v` passed.
- `go test ./... -run 'TestAttachLocalPDFToPaper(SanitizesPaperIDFileName|CopiesValidPDFSecurely)|TestProcessDownloadJobSanitizesCorruptFolderIDFallback|TestSyncKeyForPaperSanitizesFallbackPaperFileName|TestPrepareSyncSnapshotSkipsExternalPaperPDFPaths' -v` passed.
- `go test ./... -run 'TestAttachLocalPDFToPaper|TestCopyLocalPDFRejects(SourceSwapDuringOpen|FinalValidationFailure)|TestUploadScreeningFilesRejects(OversizedPDF|SymlinkPDF)' -v` passed after local PDF source symlink, check/open race, and final copied-temp validation hardening.
- `go test ./... -run 'TestAttachLocalPDFToPaper(RejectsOversizedPDF|CopiesValidPDFSecurely|RejectsFakePDFContent|SanitizesPaperIDFileName)|TestUploadScreeningFilesRejectsOversizedPDF' -v` passed.
- `go test ./... -run 'TestDownloadPDFToFile(FailurePreservesExistingTarget|RejectsRedirectToPrivateHost)|TestDownloadJobUsesQueuedDataPathSnapshot' -v` passed.
- `go test ./... -run TestStorageOverviewCountsOnlyRegularValidPDFs -v` passed.
- `go test ./... -run TestRealBaiduSyncE2E -v` passed with the expected default skip guard.
- `cd frontend && npm test -- --run src/lib/backend.test.ts` passed: 6 tests.
- `cd frontend && npm test -- --run src/lib/backend.test.ts` passed: 7 tests, including partial Wails binding fallback rejection.
- `go test ./... -run TestSanitizeAppConfigRemovesSecretValues -v` and `cd frontend && npm test -- --run src/lib/backend.test.ts` passed after expanded config redaction coverage.
- `go test ./... -run 'Test(LoadAppConfig|SaveAndLoadAppConfig|SanitizeAppConfig)' -v` passed, including oversized config/seed and symlinked config/seed coverage.
- `go test ./... -run 'TestReadLimitedFileRejectsFileSwapDuringOpen|TestLoadAppConfigRejectsSymlinkedConfigFile|TestLoadBaiduTokenRejectsUnsafeFiles' -v` passed.
- `go vet ./...` passed.
- `bash scripts/secret_scan.sh` passed.
- `bash scripts/history_secret_audit.sh` intentionally reports historical findings until destructive history cleanup is approved and executed.
- `go build ./...` passed.
- `go test -race ./...` passed. macOS linker emitted a benign `LC_DYSYMTAB` warning.
- `go test ./... -run 'TestPreprocessDeepStartResults(WritesMarkdownCacheAtomically|SanitizesSessionCacheDirectory)' -v` passed.
- `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests` passed: 3 tests.
- `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests` passed after temp-file permission/fsync and redirect SSRF coverage: 4 tests.
- `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests` passed after signed URL log/error redaction coverage: 5 tests.
- `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests` passed after upload filename redaction and uppercase extension coverage: 6 tests.
- `go test ./... -run 'TestPDFServiceClientExtractContent|TestAppExtractPaperContentReportsMissingLLMKeyBeforeExtractCall|TestAppScreeningFlowImportsSelectedPapers' -v` passed.
- `go test ./... -run 'TestPDFServiceClient(ParseAndExtractContracts|ParsePDFStreamsMultipartBody|HonorsCanceledContext)' -v` passed.
- `go test ./... -run 'TestPDFServiceClient(ParseAndExtractContracts|ParsePDFStreamsMultipartBody|RejectsUnsafeUploadFilesBeforeRequest|HonorsCanceledContext|LimitsServiceErrorBody|LimitsSuccessfulResponseBody)' -v` passed.
- `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed after PDF extraction LLM key handling coverage: 8 tests.
- `cd frontend && npm test -- --run` passed: 25 tests.
- `cd frontend && npm test -- --run` passed after latest backend/fallback coverage: 27 tests.
- `cd frontend && npm test -- --run` passed after Settings credential handling coverage: 29 tests.
- `cd frontend && npm test -- --run src/lib/backend.test.ts` passed after secret prefill value dropping coverage: 9 tests.
- `cd frontend && npm test -- --run src/components/settings/SettingsPanel.test.tsx` passed after Settings draft preservation coverage: 1 test.
- `cd frontend && npm test -- --run src/lib/backend.test.ts` passed after removing dev/mock local secret seed loading: 10 tests.
- `cd frontend && npm test -- --run` passed after removing dev/mock local secret seed loading: 30 tests.
- `cd frontend && npm test -- --run src/pages/Sync.test.tsx` passed after legacy `completed` Sync progress handling coverage: 3 tests.
- `cd frontend && npm test -- --run src/pages/Sync.test.tsx` passed after Sync settings save-failure rollback coverage: 4 tests.
- `cd frontend && npm test -- --run src/components/deepread/DeepReadPanel.test.tsx` passed after DeepRead asset-URL-to-byte fallback coverage: 5 tests.
- `cd frontend && npm test -- --run src/components/deepstart/SessionDetailPanel.test.tsx src/components/deepread/DeepReadPanel.test.tsx` passed after explicit noopener link hardening: 15 tests.
- `cd frontend && npm test -- --run` passed after Sync progress/settings status handling coverage: 32 tests.
- `rg -n "config/strong_llm|config/weak_llm|baiduyun_token|__DIVEEND_ROOT__|/@fs|fetchMockJSON" frontend/src frontend/vite.config.ts` returned no matches.
- `go test ./... -run 'TestLLMClientRejectsOversizedResponseBody|TestPDFServiceClientLimitsServiceErrorBody' -v` passed.
- `rg -n "io\\.ReadAll\\(resp\\.Body\\)|ReadAll\\(resp\\.Body\\)" *.go` returned no matches.
- `go test ./... -run 'TestBaiduPCSClientLimitsJSONResponses|TestPDFServiceClientLimitsSuccessfulResponseBody|TestPDFServiceClientLimitsServiceErrorBody' -v` passed.
- `rg -n "json\\.NewDecoder\\(resp\\.Body\\)|io\\.ReadAll\\(resp\\.Body\\)|ReadAll\\(resp\\.Body\\)" *.go` returned no matches.
- `go test ./... -run 'TestBaiduPCSClient(UploadListAndDownloadContracts|DownloadFailurePreservesExistingFile|DownloadSilentTruncationPreservesExistingFile|RejectsOversizedRemoteDownload)' -v` passed.
- `go test ./... -run 'TestSyncDownloadKeyForRemotePathRejectsUnsupportedKeys|TestSyncOnStartupSkipsUnsupportedRemoteRootFiles|TestResolveConflictRejectsUnsupportedRemotePath|TestResolveConflictRejectsPaperTargetOutsideManagedPapers|TestResolveConflictRejectsPaperTargetWithoutMatchingMetadata' -v` passed.
- `go test ./... -run 'TestSyncProgressActiveTreatsLegacyCompletedAsTerminal|TestSyncOnStartupSkipsUnsupportedRemoteRootFiles' -v` passed.
- `go test ./... -run TestPendingLocalUploadCountUsesLatestSuccessfulUploadTimes -v` passed.
- `go test ./... -run 'TestSyncOnStartupRejectsInvalidRemotePaperPDF|TestResolveConflictRemotePreservesExistingPaperWhenDownloadedPDFInvalid' -v` passed.
- `go test -race ./...` passed after Sync remote path hardening. macOS linker emitted the known benign `LC_DYSYMTAB` warning.
- `cd frontend && npm run build` passed.
- `cd frontend && npm test -- --run` and `cd frontend && npm run build` passed after removing the unused legacy DeepStart selection component.
- `cd frontend && npm audit --omit=dev --json` passed with 0 production vulnerabilities.
- `wails build` passed and produced `build/bin/DiveEnd.app/Contents/MacOS/DiveEnd`; binding generation completes without the previous `time.Time` warnings.
- 2026-06-11 final local regression passed: `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, `go test -race ./...`, `cd frontend && npm test -- --run`, `cd frontend && npm run build`, and `wails build`.
- 2026-06-12 Baidu path hardening follow-up passed: `go test ./... -run 'TestBaiduPCSClient(RejectsDeletingAppRootBeforeRequest|ListFilesSkipsUnexpectedRemotePaths|UploadListAndDownloadContracts|RejectsRemotePathEscapesAcrossOperations)' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 remote database restore staging follow-up passed: `go test ./... -run 'TestResolveConflictRemoteDatabase(CreatesRestoreDirectoryAndPendingMarker|RejectsSymlinkedRestoreDirectoryBeforeDownload)' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 local PDF import hardening follow-up passed: `go test ./... -run 'TestAttachLocalPDFToPaper|TestCopyLocalPDFRejects(SourceSwapDuringOpen|FinalValidationFailure)|TestUploadScreeningFilesRejects(OversizedPDF|SymlinkPDF)' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 managed PDF target directory hardening follow-up passed: `go test ./... -run 'TestAttachLocalPDFToPaperRejectsSymlinkedManagedFolder|TestProcessDownloadJobRejectsSymlinkedManagedFolder|TestSyncDownloadManagedPaperPDFRejectsSymlinkedTargetDirectory|TestCopyLocalPDFRejectsSymlinkedTargetDirectory|TestDownloadPDFToFileRejectsSymlinkedTargetDirectoryBeforeRequest' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 Sync staging and atomic output directory hardening follow-up passed: `go test ./... -run 'TestWriteFileAtomicRejectsSymlinkedTargetDirectory|TestPrepareSyncSnapshotRejectsSymlinkedStagingRoot|TestPrepareSyncSnapshotWritesSecureAtomicFiles|TestBaiduPCSClientDownloadRejectsSymlinkedOutputDirectoryBeforeRequest|TestBaiduPCSClientDownloadFailurePreservesExistingFile' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 DeepStart cache and restore copy hardening follow-up passed: `go test ./... -run 'TestPreprocessDeepStartResults(WritesMarkdownCacheAtomically|SanitizesSessionCacheDirectory|RejectsSymlinkedCacheRoot)|TestPendingDatabaseRestoreRejectsSymlinkBackupEscape|TestCopyFileAtomicFailurePreservesTarget|TestDatabaseRestoreScheduleAndApply' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-12 Screening storage and managed cleanup hardening follow-up passed: `go test ./... -run 'TestUploadScreeningFilesRejects(SymlinkPDF|SymlinkedStorageRoot|OversizedPDF)|TestCancelScreening(RemovesManagedSessionFiles|PreservesLibraryReferencedFiles|RejectsSymlinkedStorageRoot)|TestDeleteFolderNodeDoesNotRemove' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-13 Python PDF magic-header validation follow-up passed: `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 10 tests; `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` also passed.
- 2026-06-13 PDF extraction boundary hardening follow-up passed: `go test ./... -run 'TestPDFServiceClient(ParseAndExtractContracts|RejectsOversizedExtractionMarkdownBeforeRequest|LimitsSuccessfulResponseBody|LimitsServiceErrorBody)' -v`, `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 12 tests, plus `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-13 PDF service startup guidance follow-up passed: `go test ./... -run TestParsePDFFriendlyErrorWhenServiceUnavailable -v`, `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...`.
- 2026-06-13 frontend user-visible error redaction follow-up passed: `cd frontend && npm test -- --run src/lib/errors.test.ts src/stores/appStore.test.ts src/lib/backend.test.ts`, `cd frontend && npm test -- --run`, `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 Python PDF service log/error redaction follow-up passed: `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 15 tests, plus `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 Go external error-body redaction follow-up passed: `go test ./... -run 'TestRedactSensitiveText|TestLLMClientRedactsErrorResponseBody|TestPDFServiceClientRedactsServiceErrorBodies|TestSearchClientRedactsProviderErrorBodies' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 Baidu request URL token redaction follow-up passed: `go test ./... -run 'TestBaiduPCSClientRedactsTokenQueryFromNetworkErrors|TestBaiduRefreshTokenRedactsCredentialQueryFromNetworkError' -v`, `go test ./... -run 'TestBaiduPCSClient|TestBaiduRefreshToken|TestRedactSensitiveText' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 Baidu decoded failure payload redaction follow-up passed: `go test ./... -run 'TestBaiduPCSClientRedactsDecodedFailureMaps|TestBaiduPCSClientRedactsTokenQueryFromNetworkErrors|TestBaiduRefreshTokenRedactsCredentialQueryFromNetworkError|TestBaiduPCSClientUploadLogsDoNotExposeSensitivePathsOrIDs' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 Python extraction partial-parse log redaction follow-up passed: `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 16 tests, plus `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 DeepStart runtime message redaction follow-up passed: `cd frontend && npm test -- --run src/components/deepstart/SessionDetailPanel.test.tsx`, `cd frontend && npm test -- --run`, `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 Search progress/log query redaction follow-up passed: `go test ./... -run 'TestRedactSensitiveText|TestRedactURLQueryValuesInText|TestSearchClientRedactsProgressLogsAndAggregatedErrors|TestSearchClientRetryStopsAfterPerSourceSuccessAndEmitsProgress' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 PDF extraction config-error echo redaction follow-up passed: `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 17 tests, plus `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 frontend local error-state redaction follow-up passed: `cd frontend && npm test -- --run src/components/settings/SettingsPanel.test.tsx src/components/deepread/DeepReadPanel.test.tsx src/components/deepstart/SessionDetailPanel.test.tsx`, `cd frontend && npm test -- --run`, `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 PaperList error redaction follow-up passed: `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 2 tests, covering create-folder and delete-paper error redaction. Full gates also passed: `cd frontend && npm test -- --run` (48 tests), `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `go test -race ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 LLM client network/config error redaction follow-up passed: `go test ./... -run 'TestLLMClientRedactsErrorResponseBody|TestLLMClientRedactsProviderLabelAndURLQueriesFromNetworkErrors|TestRedactURLQueryValuesInText|TestLLMClientTranslateSectionUsesResponsesAPI|TestLLMClientTranslateSectionUsesChatCompletions' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 signed PDF download network-error redaction follow-up passed: `go test ./... -run 'TestDownloadPDFToFile(RedactsSignedURLFromNetworkError|FailurePreservesExistingTarget|RejectsSymlinkedTargetDirectoryBeforeRequest|RejectsRedirectToPrivateHost)' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 DeepStart enrichment/parser unavailable disclosure follow-up passed: `go test ./... -run 'TestDeepStartEnricherRedactsMetadataRequestNetworkErrors|TestLLMClientRedactsProviderLabelAndURLQueriesFromNetworkErrors' -v`, `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 18 tests, plus `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`.
- 2026-06-13 cross-stack disclosure hardening closeout passed: `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning), `cd frontend && npm test -- --run` passed with 43 tests, `cd frontend && npm run build` passed, and `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` passed with 18 tests.
- 2026-06-13 local directory symlink hardening follow-up passed: `go test ./... -run 'TestNewDBRejectsSymlinkedDataPath|TestDatabaseRestoreRejectsSymlinkedRestoreDirectory|TestWriteFileAtomicRejectsIntermediateSymlinkedTargetDirectory|TestWriteFileAtomicRejectsSymlinkedTargetDirectory|TestPrepareSyncSnapshotWritesSecureAtomicFiles|TestDatabaseRestoreScheduleAndApply|TestDownloadPDFToFileFailurePreservesExistingTarget|TestBaiduPCSClientDownloadFailurePreservesExistingFile' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 remaining production directory creation hardening follow-up passed: `go test ./... -run 'TestScheduleDatabaseRestoreRejectsSymlinkedDataPath|TestAttachLocalPDFToPaperRejectsIntermediateSymlinkedManagedFolder|TestNewDBRejectsSymlinkedDataPath|TestWriteFileAtomicRejectsIntermediateSymlinkedTargetDirectory|TestDatabaseRestoreScheduleAndApply' -v`, `go test ./...`, `go vet ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 final cross-stack verification passed after the last directory-creation audit: `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, `go test -race ./...`, `cd frontend && npm test -- --run` (43 tests), `cd frontend && npm run build`, `wails build`, and `/Users/bytedance/python_proj/joey_venv/bin/python -m pytest services/pdf_service/tests -q` (18 tests).
- 2026-06-13 fd-relative atomic file operation hardening follow-up passed: `go test ./... -run 'TestWriteFileAtomic|TestRenameFileAtomicWithinDir|TestRemoveFileAndSyncDir|TestCopyLocalPDF|TestDownloadPDFToFile|TestBaiduPCSClientDownload|TestDatabaseRestore|TestPrepareSyncSnapshot|TestSyncDownload|TestCancelScreening|TestDeleteFolderNode' -v`, `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 fd-relative recursive cleanup follow-up passed: `go test ./... -run 'TestRemoveTreeAndSyncParent|TestRemoveFileAndSyncDir|TestWriteFileAtomic|TestRenameFileAtomicWithinDir|TestPrepareSyncSnapshot|TestDeleteFolderNode|TestCancelScreening' -v`, `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 SQLite online-backup snapshot follow-up passed: `go test ./... -run 'TestPrepareSyncSnapshot|TestSyncToCloud|TestTriggerSync|TestStartSync|TestPendingLocalUploadCount|TestRealBaiduSyncE2E' -v` passed with the real Baidu E2E default skip guard, plus `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 real Baidu E2E credential preflight follow-up: authorized live runs reached Baidu Cloud but the local `baiduyun_token.json` refresh token was rejected by Baidu with HTTP 400. `SyncToCloud` now fails before snapshot/upload work with `baidu token preflight failed`, and the opt-in E2E test fails before registering remote cleanup. Local gates still passed: `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test -race ./...` (with the known benign macOS `LC_DYSYMTAB` linker warning).
- 2026-06-13 SQLite snapshot post-processing fd hardening follow-up passed: `go test ./... -run 'TestPrepareSyncSnapshot|TestSyncToCloud|TestTriggerSync|TestStartSync|TestWriteFileAtomic|TestRemoveTreeAndSyncParent' -v`, `go test ./...`, `go vet ./...`, `go build ./...`, `bash scripts/secret_scan.sh`, `git diff --check`, `go test -race ./...`, and `wails build` (with the known benign macOS `LC_DYSYMTAB` linker warning in race tests).
- 2026-06-13 exit-time Sync lifecycle coverage follow-up passed: `go test ./... -run 'TestWaitForSyncCompletion|TestShouldPromptBeforeClose|TestTriggerSync|TestSyncProgressActive|TestPendingLocalUploadCount'`, `bash scripts/secret_scan.sh`, and `bash scripts/history_secret_audit.sh` confirmed the current tree is clean while Git history remains intentionally blocked until destructive cleanup/token rotation is approved.
- 2026-06-13 shutdown Sync pending-check follow-up passed: `go test ./... -run 'TestWaitForSyncCompletion|TestShouldPromptBeforeClose|TestShutdownSyncSkipsWhenNoLocalUploadsArePending|TestTriggerSync|TestPendingLocalUploadCount'` confirmed shutdown does not touch the Baidu HTTP client when every local sync candidate has a newer successful upload record.
- 2026-06-13 periodic auto-sync follow-up passed: `go test ./... -run 'TestSaveSyncSettingsStartsAndStopsPeriodicSyncLoop|TestPeriodicSyncDurationUsesConfiguredInterval|TestShutdownSyncSkipsWhenNoLocalUploadsArePending|TestWaitForSyncCompletion|TestShouldPromptBeforeClose'`, `go test -race ./... -run 'TestSaveSyncSettingsStartsAndStopsPeriodicSyncLoop|TestPeriodicSyncDurationUsesConfiguredInterval|TestShutdownSyncSkipsWhenNoLocalUploadsArePending'`, and `cd frontend && npm test -- --run src/pages/Sync.test.tsx` passed, covering settings-driven loop start/stop, `syncInterval` duration, pending-check behavior, and frontend interval persistence.
- 2026-06-13 conflict-resolution strategy follow-up passed: `go test ./... -run 'TestRefreshSyncConflicts(ManualStrategyStoresUnresolvedConflict|TimestampStrategyStagesRemoteDatabase)|TestResolveConflictRemoteDatabaseCreatesRestoreDirectoryAndPendingMarker' -v`, `go test -race ./... -run 'TestRefreshSyncConflicts(ManualStrategyStoresUnresolvedConflict|TimestampStrategyStagesRemoteDatabase)|TestSaveSyncSettingsStartsAndStopsPeriodicSyncLoop'`, and `cd frontend && npm test -- --run src/pages/Sync.test.tsx` passed, covering manual conflict preservation, timestamp auto-resolution through pending DB restore staging, and frontend strategy persistence.
- 2026-06-13 conflict history/pending follow-up passed: `go test ./... -run 'TestResolveConflict(LocalDatabaseRecordsUploadAndClearsPending|RemoteDatabaseCreatesRestoreDirectoryAndPendingMarker)|TestRefreshSyncConflicts(TimestampStrategyStagesRemoteDatabase|ManualStrategyStoresUnresolvedConflict)|TestSyncLocalFileModifiedAfterUploadIgnoresDatabaseRecordWriteGrace' -v` and `go test -race ./... -run 'TestResolveConflictLocalDatabaseRecordsUploadAndClearsPending|TestSyncLocalFileModifiedAfterUploadIgnoresDatabaseRecordWriteGrace'` passed, covering conflict action history records and preventing sync-history writes from causing immediate DB re-upload loops.
- 2026-06-13 Sync history message follow-up passed: `go test ./... -run 'TestResolveConflict(LocalDatabaseRecordsUploadAndClearsPending|RemoteDatabaseCreatesRestoreDirectoryAndPendingMarker)|TestSyncLocalFileModifiedAfterUploadIgnoresDatabaseRecordWriteGrace' -v` and `cd frontend && npm test -- --run src/pages/Sync.test.tsx` passed, covering conflict-resolution history messages and frontend display.
- 2026-06-13 legacy Sync history migration follow-up passed: `go test ./... -run 'TestNewDBMigratesLegacy(SchemaAndCreatesDefaultFolder|SyncRecordsMessageColumn)' -v` and `go test -race ./... -run 'TestNewDBMigratesLegacySyncRecordsMessageColumn'` passed, covering in-place migration from pre-message `sync_records` tables and message round-trip on new records.
- 2026-06-13 conflict refresh idempotency follow-up passed: `go test ./... -run 'TestRefreshSyncConflictsManualStrategy(PreservesStableConflictID|StoresUnresolvedConflict)|TestSyncLocalFileModifiedAfterUploadIgnoresDatabaseRecordWriteGrace' -v` and `go test -race ./... -run 'TestRefreshSyncConflictsManualStrategyPreservesStableConflictID'` passed, covering stable conflict IDs and no-op refresh behavior for unchanged manual conflicts.
- 2026-06-13 resolved-conflict visibility follow-up passed: `go test ./... -run 'TestGetSyncConflictsHidesResolvedCachedConflicts|TestRefreshSyncConflictsManualStrategy(PreservesStableConflictID|StoresUnresolvedConflict)' -v` and `go test -race ./... -run 'TestGetSyncConflictsHidesResolvedCachedConflicts'` passed, covering resolved cached conflicts staying out of API results and status counts.
- 2026-06-13 upload-batch database baseline follow-up passed: `go test ./... -run 'TestSyncToCloudFinalizesDatabaseUploadRecordAfterBatch|TestTriggerSyncPublishesProgressSnapshots|TestPendingLocalUploadCountUsesLatestSuccessfulUploadTimes' -v` and `go test -race ./... -run 'TestSyncToCloudFinalizesDatabaseUploadRecordAfterBatch'` passed, covering batch-end database upload finalization and avoiding immediate DB pending after a successful asset/manifest upload batch.
- 2026-06-13 periodic Sync settings responsiveness follow-up passed: `go test ./... -run 'TestSaveSyncSettings(StartsAndStopsPeriodicSyncLoop|DoesNotWaitForStoppingPeriodicSyncWorker)|TestPeriodicSyncDurationUsesConfiguredInterval' -v` and `go test -race ./... -run 'TestSaveSyncSettingsDoesNotWaitForStoppingPeriodicSyncWorker'` passed, covering non-blocking settings saves while retaining wait-on-shutdown behavior.
- 2026-06-13 periodic Sync lifecycle serialization follow-up passed: `go test ./... -run 'TestSaveSyncSettings(StartsAndStopsPeriodicSyncLoop|DoesNotWaitForStoppingPeriodicSyncWorker)|TestPeriodicSyncStopAndReconfigureAreSerialized' -v` and `go test -race ./... -run 'TestSaveSyncSettingsDoesNotWaitForStoppingPeriodicSyncWorker|TestPeriodicSyncStopAndReconfigureAreSerialized'` passed, covering concurrent stop/reconfigure behavior without unsafe wait-group interleavings.
- 2026-06-11 security follow-up passed: after removing the legacy Baidu manual test script/template from tracking and expanding the blocked-file guardrails, `bash scripts/secret_scan.sh`, `git diff --check`, and `go test ./...` passed; `git ls-files` no longer reports `baidupan_test.py`, `baidu_pcs.go.tmpl`, or `downloaded.txt`.
- 2026-06-13 closeout verification passed after resolved-conflict visibility and periodic Sync lifecycle follow-ups: `go test ./...`, `go vet ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (45 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, `git diff --check`, and `wails build`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 exit-time Sync toggle split follow-up passed: `go test ./... -run 'TestShouldPromptBeforeCloseRequiresConfiguredExitSyncAndDatabase|TestShutdownSyncSkipsWhenNoLocalUploadsArePending|TestSaveSyncSettingsStartsAndStopsPeriodicSyncLoop' -v`, `go test ./... -run 'TestAppSaveConfig' -v`, `cd frontend && npm test -- --run src/pages/Sync.test.tsx` (7 tests), `wails build`, `go test ./...`, `go vet ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (46 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check` passed. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 folder deletion consistency follow-up passed: `go test ./... -run 'TestDeleteFolderNode' -v` now covers external PDF skip, corrupt folder-ID path escape skip, symlinked managed root rejection, and symlinked intermediate managed parent rejection while preserving DB rows on cleanup failure. Full gates also passed: `go test ./...`, `go vet ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (46 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 single-paper deletion cleanup follow-up passed: `go test ./... -run 'TestDeletePaper' -v` covers unshared managed PDF cleanup, shared managed PDF preservation, external PDF preservation, and symlinked managed root rejection with DB row preservation. Full gates also passed: `go test ./...`, `go vet ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (46 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 PaperList folder delete UI follow-up passed: `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 3 tests, covering non-system folder deletion, descendant removal from frontend state, active-folder fallback, and selected-paper clearing. Full gates also passed: `cd frontend && npm test -- --run` (49 tests), `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `go test -race ./...`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 folder rename follow-up passed: `go test ./... -run 'TestRenameFolderNode' -v` covers managed directory moves, descendant path updates, paper/cache PDF path updates, and system-folder rename rejection; `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 4 tests, covering PaperList rename refresh behavior. `wails build` regenerated bindings successfully. Full gates also passed: `go test ./...`, `go vet ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (50 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 folder move follow-up passed: `go test ./... -run 'Test(MoveFolderNode|RenameFolderNode)' -v` covers cross-parent managed directory moves, descendant path updates, paper/cache PDF path updates, descendant-cycle rejection, path-conflict rejection, and system-folder move rejection; `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 7 tests, covering PaperList folder move refresh behavior. `wails build` regenerated bindings with `MoveFolderNode`. Full gates also passed: `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (53 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 single-paper move follow-up passed: `go test ./... -run 'TestMovePaperToFolder' -v` covers managed PDF moves, DeepRead cache path updates, shared old managed PDF preservation, external PDF preservation, and duplicate-source rejection; `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 5 tests, covering PaperList move refresh behavior. `wails build` regenerated bindings with `MovePaperToFolder`. Full gates also passed: `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (51 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-13 batch paper move follow-up passed: `go test ./... -run 'TestMovePaper' -v` covers single and batch paper moves, including shared managed PDF copy-once behavior, DeepRead cache updates, duplicate-source preflight without partial moves, and target PDF filename collision preflight; `cd frontend && npm test -- --run src/components/paperlist/PaperListPanel.test.tsx` passed with 6 tests, covering batch selection and move refresh behavior. `wails build` regenerated bindings with `MovePapersToFolder`. Full gates also passed: `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (52 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, and `git diff --check`. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-14 Sync conflict diff/version-preservation follow-up passed: `go test ./... -run 'TestResolveConflict|TestRefreshSyncConflicts|TestGetSyncConflicts|TestSyncAPIsReturnCachedConflictsDuringInProgressSync|TestNewDBMigratesLegacySync(RecordsMessageColumn|ConflictDiffColumns)' -v`, `cd frontend && npm test -- --run src/pages/Sync.test.tsx`, `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...`, `cd frontend && npm test -- --run` (54 tests), `cd frontend && npm run build`, `bash scripts/secret_scan.sh`, `git diff --check`, and `wails build` passed. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- 2026-06-14 frontend operation/error UX follow-up passed: `cd frontend && npm test -- --run src/App.test.tsx src/components/deepread/DeepReadPanel.test.tsx src/components/deepstart/SessionDetailPanel.test.tsx`, `cd frontend && npm test -- --run src/pages/Screening.test.tsx`, `cd frontend && npm test -- --run` (59 tests), `cd frontend && npm run build`, `go test ./...`, `go vet ./...`, `go build ./...`, `go test -race ./...`, `bash scripts/secret_scan.sh`, and `git diff --check` passed. Race tests still emit the known benign macOS `LC_DYSYMTAB` linker warning.
- Vite HTTP smoke check passed on `http://127.0.0.1:5175/`.
- In-app browser route smoke passed on `http://127.0.0.1:5177/`: Home, DeepStart, DeepRead, Screening, and Sync loaded with key controls visible and no console errors. Sync page screenshot captured at `/private/tmp/diveend-2026-06-11-sync-smoke.png`.
- In-app browser interaction smoke passed: DeepStart accepted a query, entered a mock session route, rendered AI overview/category/local storage panels, and produced no console errors.
- Authorized real Baidu E2E execution reached the live Baidu API. Earlier live runs exposed numeric `fs_id` handling for manifest download and false-positive Sync completion after upload failures; both are fixed and covered by local mock tests. The latest live run is blocked by the local `baiduyun_token.json` refresh token being rejected by Baidu with HTTP 400 during preflight. A final live pass requires a freshly valid Baidu token file.

## Remaining Risks

- Git history still contains previously committed local files, legacy Baidu scratch script/template paths, and at least one historical OpenAI-style key pattern. Current tracking is fixed, but historical purge and token rotation must be performed intentionally before any public push.
- Wails desktop bridge delivery for `runtime.EventsEmit` still needs authorized manual/app execution. The backend event-emission handoff, backend progress source, Wails production packaging, frontend browser smoke, BaiduPCS mock protocol, and an opt-in real Baidu E2E entry point now all have automated coverage. Final real Baidu Cloud account sync is currently blocked only by the local token file's refresh token being rejected by Baidu during preflight.
- Managed file writes/replacements/removals, managed recursive cleanup, and Sync database snapshot post-processing/commits now use directory-fd-relative `openat`/`renameat`/`unlinkat` with inode stability checks. The previous SQLite `VACUUM INTO` snapshot flow has been replaced with sqlite3 online backup into an already-created secure temp file; the SQLite driver still opens that temp file by path, but validation, sync, cleanup, and final commit stay anchored to the original directory fd.
