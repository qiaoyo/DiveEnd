# DiveEnd - 前端 UI/UX 设计方案 🎨

> **项目名称**: DiveEnd - 沉浸式论文收集与AI翻译阅读工具
> **设计理念**: 沉浸式、学术感、AI驱动、工作流清晰
> **设计风格**: 极简主义 + 暗/浅色护眼模式 + 侧边栏主导航

基于 DiveEnd 的核心工作流（探索 -> 筛选 -> 沉浸阅读 -> 同步管理），本方案为您规划了一套符合 **Tailwind CSS + React** 技术栈的现代、高颜值界面设计。

---

## 🎨 全局设计系统 (Design System)

构建统一的视觉语言，确保应用的高级感与一致性。

*   **主色调 (Primary)**: `Indigo-600` (#4F46E5) - 传达学术、专注、科技感。
*   **AI 元素色 (AI Accent)**: `Violet-500` (#8B5CF6) - 专属用于 AI 生成、总结、分类的高亮与图标，与常规操作区分。
*   **背景色 (Background)**:
    *   **浅色模式 (Light)**: `Slate-50` (#F8FAFC) - 柔和护眼，避免纯白刺眼。
    *   **深色模式 (Dark)**: `Slate-900` (#0F172A) - 沉浸式暗黑，适合夜间深度阅读。
*   **排版 (Typography)**:
    *   标题/UI文字: *Inter* 或 *Geist* (现代无衬线体，清晰易读)。
    *   正文/阅读区: *Merriweather* (衬线体，缓解长篇学术论文的阅读疲劳)。
*   **布局骨架**: 左侧全局极窄边栏（仅图标导航） + 中间主内容区 + 侧边/悬浮操作面板。

---

## 🖼️ 核心页面视觉规划

### 1. DeepStart (领域探索与导入页)
**🎯 工作流节点：搜索论文 ➔ AI 分类 ➔ 挑选 ➔ 导入**

**【视觉与布局】**
*   **顶部 (Search Header)**: 一个极简且巨大的“类 Google”全局搜索框，采用 **毛玻璃效果 (Glassmorphism)**。输入框右侧带有发光的 ✨ AI 小图标，提示用户支持自然语言模糊搜索（例："寻找关于大模型长文本处理的最新综述"）。
*   **主体内容 (左右分栏布局)**:
    *   **左栏 (Category Tree, 占比 30%)**: 搜索后生成的 AI 多级分类树。采用柔和的贝塞尔曲线连接节点，每个节点带有微型徽章显示该分类下的论文数量。支持点击过滤。
    *   **右栏 (Search Results, 占比 70%)**: 论文卡片瀑布流/列表。每张卡片包含：标题、作者、会议/期刊标签，以及醒目的由 **Weak LLM** 生成的【一句话 AI 总结】。
*   **底部悬浮 (Floating Action Bar)**: 当用户勾选多篇论文时，底部平滑滑出一个深色悬浮条，显示“已选 N 篇”，右侧为抢眼的 `[📥 一键导入工作区]` 主按钮。

> **👨‍💻 开发者提示**: 参考 Tailwind 的 `backdrop-blur-md` 实现搜索框毛玻璃效果；卡片悬浮使用 `hover:shadow-lg hover:-translate-y-1` 增加可交互感。

---

### 2. Screening Pipeline (论文筛选与处理页)
**🎯 工作流节点：上传 PDF ➔ 提取内容 ➔ 决策树筛选 ➔ 结果展示**

**【视觉与布局】**
*   **顶部 (Pipeline 进度条)**: 类似电商物流的水平步骤条，清晰展示当前状态：`1. 队列 (Queue)` ➔ `2. 解析 (Extracting)` ➔ `3. 筛选 (Screening)` ➔ `4. 结果 (Results)`。
*   **主体视图 (分区块设计)**:
    *   **左侧 (Upload & Progress)**: 带有虚线边框和拖拽提示的上传区。下方为动态任务列表，显示正在解析的 PDF 文件，配套环形进度条（实时对接 Wails 推送的 Events）。
    *   **右侧 (AI 决策树交互 UI)**: 本页面的灵魂。采用类似流程图或对话卡片的界面。
        *   **AI 提问卡片**: 例如 *"这篇论文是否包含真实世界的评估数据集？"*
        *   **操作区**: 提供 `[✔️ 是]`、`[❌ 否]`、`[⏭️ 跳过]` 的胶囊按钮。
        *   **全自动模式展示**: 采用 **漏斗图 (Funnel Chart)**，被淘汰的论文卡片带有下降动画并变灰半透明，保留的打上绿色 Check 标记。

> **👨‍💻 开发者提示**: 进度条和卡片丢弃效果强烈建议使用 `Framer Motion` 实现平滑的物理动画。

---

### 3. DeepRead (沉浸式阅读页) - 🌟 核心页面
**🎯 工作流节点：分屏阅读 ➔ AI 翻译 ➔ 划线高亮 ➔ 记笔记**

**【视觉与布局】**
*   **布局骨架**: 经典的 **三栏布局 (3-Column Layout)**，阅读模式下隐藏所有多余全局导航，实现 100% 沉浸。
    *   **左栏 (大纲视图 15%)**: 可收缩的 PDF 目录树和页面缩略图。
    *   **中栏 (PDF 原文区 45%)**: 原生 PDF 渲染。顶部悬浮极简工具栏（缩放、荧光笔）。背景色智能匹配文档纸张颜色。
    *   **右栏 (AI 助手与笔记 40%)**: 支持上下拖拽调整比例的双面板：
        *   *上面板 (Translation)*: 当用户在 PDF 中划选段落时，此处平滑切入由 **Strong LLM** 提供的精准翻译，支持中英段落高亮对照。
        *   *下面板 (Chat & Notes)*: 论文专属 AI 对话框。用户提问（"总结该文创新点"）后，AI 的回答可一键转换为 Markdown 笔记保存。
*   **专注模式 (Zen Mode)**: 提供一键切换按钮，点击后左右两栏淡出，屏幕仅保留 PDF 文档和选词时的悬浮翻译气泡。

> **👨‍💻 开发者提示**: 三栏拖拽缩放推荐使用 `react-resizable-panels`；滚动条需全局定制为极细且半透明（`::-webkit-scrollbar`）。

---

### 4. Sync & Polish (同步与数据面板)
**🎯 工作流节点：云端同步 ➔ 状态监控 ➔ 冲突解决**

**【视觉与布局】**
*   **顶部 Widget (状态仪表盘)**: 
    *   数据卡片：本地论文总数、占用空间大小。
    *   状态卡片：**百度云连接状态**（带呼吸灯效果，绿灯 Connected），Token 状态。
    *   操作区：醒目的 `[🔄 开始同步]` 按钮（点击后带旋转过渡动画）。
*   **核心内容区 (Tabs 切换)**:
    *   **Tab 1 - 同步历史 (History)**: 类似 Git Commit 记录的时间线（Timeline）UI，清晰展示每次同步增加/修改了哪些文献。
    *   **Tab 2 - 冲突解决 (Conflict Resolution)**: 双栏对比视图 (Diff View)。
        *   左侧：`本地版本 (最后修改: 今天 10:00)` - 差异部分红色高亮。
        *   右侧：`云端版本 (最后修改: 昨天 22:00)` - 差异部分绿色高亮。
        *   中央控制：提供悬浮按钮 `[← 覆盖本地]` 或 `[覆盖云端 →]`。
*   **底部卡片 (Preferences)**: 自动同步设置开关（如："启动时自动同步"、"退出前检查未同步项"）。

---

## 💡 前端技术落地建议

基于您当前的架构（**React + TypeScript + Tailwind CSS + Vite**），建议引入以下现代化工具库来加速高质量 UI 的构建：

1.  **UI 基础组件库**: 推荐使用 **[shadcn/ui](https://ui.shadcn.com/)**。它基于 Tailwind 构建，样式极简且代码完全属于你（可随意定制），非常适合构建 DeepRead 的卡片、弹窗和 Screening 的进度条。
2.  **动画引擎**: **[Framer Motion](https://www.framer.com/motion/)**。在论文提取阶段、AI 决策树卡片切换、页面路由切换时加入微动画，极大提升软件的“客户端级”质感。
3.  **图标系统**: **[Lucide React](https://lucide.dev/)**。线条干净、一致性强，非常契合学术和生产力工具的调性。
4.  **可拖拽布局**: 使用 **[react-resizable-panels](https://github.com/bvaughn/react-resizable-panels)** 实现 DeepRead 的三栏自由拖拽调整比例功能。

---
*DiveEnd - 让每一篇论文的阅读都成为一次沉浸式的探索。*